---
skill: psm-setup
intent: edit
updated: 2026-07-16T12:41
---

- (direction by user) Edit (BW), lanjutan dari psm-setup pasangan psm-validate: default versi/tag flashlight usang di SUMBER default (bukan config project yg sudah benar). module.yaml default 9.0/8.1=8.1 + nightly; resolve-psm-config.py PSM_DEFAULTS 9.0=nightly (tp 8.1 sudah benar 8.1.6-nginx -> inkonsisten internal); README tabel 9.0/nightly. Arah: samakan SEMUA default kanonik ke 1.7.8=1.7.8.11,8.1=8.1.6-nginx,9.1=9.1.4-nginx & target 1.7.8,8.1,9.1; update test-resolve in-lockstep. Alasan: tanpa ini, setup project BARU tetap lahir 9.0/nightly (broken).
- (event) edit selesai: default kanonik disamakan ke config. module.yaml default psm_target_versions 1.7.8,8.1,9.1 + tag map 1.7.8=1.7.8.11,8.1=8.1.6-nginx,9.1=9.1.4-nginx. resolve-psm-config.py PSM_DEFAULTS idem (9.0=nightly->9.1=9.1.4-nginx, tuntaskan inkonsistensi internal 8.1). README tabel idem. test-resolve disinkronkan (fixture+asersi 9.0->9.1). Test 'semua lolos'. Resolver sanity OK. Lint: quick_validate ok:false HANYA krn name:"psm-setup" berkutip (PRE-EXISTING di HEAD, bukan dari edit ini); scan-scripts 3 med & scan-path 17 non-analysis (README5/SKILL11/memlog1) SEMUA pre-existing, nol di file yg kuubah. SKILL.md tak disentuh. Sisa 'core-updates 8.0/9.0' hanya di psm-validate ps-rules.json (sitiran doc).
- (event) fix pre-existing: name:"psm-setup" berkutip -> name: psm-setup (hyphen-case). quick_validate kini ok. Isolated: 6 skill psm lain sudah unquoted.
- (event) lint cleanup pre-existing: SKILL.md normalisasi 14 path ./assets|./scripts -> bare (samakan gaya dgn psm-validate); reword contoh absolute-path /home/me/myapp -> deskriptif. scan-path non-analysis 17->7; sisa 7 sengaja ditahan: README bare-_bmad x4 (+1 structure) = docs manusia biar terbaca, SKILL:62 bare-_bmad prosa deskriptif, .memlog structure false-pos. quick_validate ok. BELUM disentuh: scan-scripts 3 med (cleanup-legacy/merge-config/merge-help-csv tanpa unit test) = tugas terpisah lebih besar.
- (event) config flashlight: +4 default kanonik PSM_DEFAULTS (psm_flashlight_orchestrator=auto, db_image=mariadb:lts, ps_domain=localhost:8000, startup_timeout=180) — resolver kenali langsung, override via config.yaml, TANPA prompt baru (jaga filosofi consolidated-defaults, tak nambah pertanyaan setup). README tabel +4 baris. test-resolve +4 assert. Test lolos, quick_validate ok.
- (event) tutup scan-scripts 3 med (pre-existing): tambah unit test 3 skrip installer — test-merge-config.py (apply_result_templates anti-double-prefix, merge_config anti-zombie+migrasi core+strip user-key, extract_user_settings, apply_legacy_defaults, metadata, reject_unresolved_paths), test-merge-help-csv.py (extract_module_codes, filter_rows anti-zombie, roundtrip CSV, komposisi merge, reject-paths), test-cleanup-legacy.py (find_skill_dirs, verify_skills_installed installed+missing-exit, cleanup_directories hitung file rekursif+not_found, reject-paths). Semua lolos. scan-scripts psm-setup kini 0/0/0.
- (event) rapikan SKILL.md:62 bare-_bmad -> {project-root}/_bmad/ (prosa cleanup-legacy). SKILL.md kini 0 temuan path-standards. Sisa non-analysis sengaja ditahan: README bare-_bmad x5 (docs manusia, _bmad/config.yaml justru path yg user ketik) + .memlog x4 (false-pos builder immutable). quick_validate ok.
- (event) analyze: grade good, 0 critical / 1 high / 11 medium / 7 low, report .analysis/2026-07-16-1201/skill-analysis-report.html
- (event) fix se-keluarga (tindak lanjut analyze psm-scaffold 2026-07-16-1159): (1) Seed KB repoint katalog sibling ke <skills-dir> alih-alih mirror skills/; (2) mirror skills/ dipangkas — 6 salinan skill basi dihapus dari working tree (recoverable via git), tersisa skills/psm-setup (resolver runtime + README kanonik dirujuk README root) + skills/reports; (3) BELUM direkonsiliasi: drift dua arah psm-setup live vs mirror (live lebih baru — commit 9b04ff8; mirror punya references/README.md + plan-setup.py + test-plan-setup.py yang tak ada di live) — butuh keputusan Budi.
- (event) koreksi entri sebelumnya: konvensi resolver BERUBAH — keenam SKILL.md live kini memanggil resolver di {project-root}/.claude/skills/psm-setup/scripts/ (pohon live); skills/psm-setup/scripts/ tak lagi punya peran runtime. Alasan tersisa mempertahankan mirror skills/psm-setup: README.md kanonik (dirujuk README root) + skills/reports. Drift dua arah psm-setup tetap belum direkonsiliasi.
- (event) fix pass: 19/19 temuan analyze 2026-07-16-1201 ditutup. merge-config: +--show-defaults (install_type+provenance), tier preserve existing (update tak reset nilai tak-di-echo), buat directories: dari module.yaml (determinism-1..3). cleanup-legacy: +--preserve memory/.memlog.md (bonus: tutup tabrakan urutan yang menghapus KB ter-seed saat re-run). SKILL.md rewrite: Resolution rules, dedupe token-rule/user-key, seed hand-off minimal EN name-neutral, headless return (memlog assumption+JSON), 2124->1931 token. Resolver dikonsolidasi: 6 konsumen+QUICKSTART -> .claude/skills/, salinan scaffold dihapus, +test anti-drift PSM_DEFAULTS<->module.yaml. README direlokasi docs/psm/README.md (+2 key E2E, sumber kebenaran, 'balas pakai default semua'). module_greeting netral. Semua test 4 suite lolos, smoke E2E fresh->update->cleanup OK, scanner: SKILL/assets/scripts 0 temuan.
- (note) sejarah pra-konsolidasi (dari skills/psm-setup/.memlog.md yang dihapus 2026-07-16, utuh di git history): 6 entri 8-9 Jul — analyze good (0c/1h/2m/2l) -> fix pass menambah plan-setup.py (klasifikasi install-state + resolusi default) -> analyze rerun excellent -> rerun3 CLEAN PASS 0/0/0/0. plan-setup.py kemudian usang: fungsinya diserap merge-config.py --show-defaults di pohon live, dihapus bersama mirror.
- (event) rekonsiliasi drift mirror tuntas: skills/psm-setup DIHAPUS dari working tree (git-recoverable). Triase file mirror-only — plan-setup.py+test usang (diserap merge-config.py --show-defaults), references/README.md (overview lama, default 9.0/nightly basi) usang (penerus: docs/psm/README.md), README.md (panduan instalasi) BERGUNA -> direlokasi ke docs/psm/install.md dengan penyegaran (4 lapis validate + E2E, KB ecommerce 3 file, tautan silang ke README module). README root di-repoint ke docs/psm/. skills/ kini hanya berisi reports/. Salinan stale lain (SKILL.md, assets, merge scripts) tertinggal di belakang live — dibuang tanpa porting.
