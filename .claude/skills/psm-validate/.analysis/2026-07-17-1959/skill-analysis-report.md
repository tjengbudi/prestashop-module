# Analysis Report: .claude/skills/psm-validate

Generated: 2026-07-17 · Schema: 2

**Grade: Poor**

> POOR — satu CRITICAL: probe reachability Lapis 4 mengklasifikasi FO HTTP 500 sebagai kegagalan infra, menelan dua temuan konklusif yang persis jadi alasan Lapis 4 ada; dua lensa mereproduksinya secara independen, dan seam kembarnya (konklusivitas BO) menghasilkan pass=true atas module yang mem-fatal Back Office.

Empat invarian false-pass, wire config (10 key psm_*), kontrak exit-2, dan gerbang CI headless semuanya TERVERIFIKASI SEHAT lewat reproduksi — fondasi lima ronde sebelumnya bertahan, dan tak ada dead wire untuk pertama kalinya. Peluang utamanya bukan lubang baru melainkan SATU kesalahan yang sama di kedua arah: kanal 'tak konklusif' (infra) dan kanal 'vonis memblok' terus tertukar maknanya — kanal infra menelan bukti konklusif di dua seam (FO 500, BO fatal), sementara di seam ketiga infra murni dijual sebagai vonis memblok karena sentinel PSM_INSTALL_FAIL ditulis tanpa pembaca. Arah sapuan ronde ini terbukti tepat: gerbang yang benar di satu seam ternyata telanjang di kembarannya sebanyak lima kali.

| Severity | Count |
| --- | --- |
| Critical | 1 |
| High | 7 |
| Medium | 11 |
| Low | 4 |

## Themes

### 1. Kanal infra vs vonis: satu kesalahan, dua arah, tiga seam

- Root cause: Skill ini punya dua kanal dengan makna berlawanan — 'tak konklusif' (degrade jujur, tak memblok) dan 'temuan konklusif' (memblok). Bukti terus dikirim ke kanal yang salah, di KEDUA arah. Arah pertama: bukti konklusif ditelan kanal infra — probe FO membaca HTTP 500 lalu menuduh 'port publish/boot gagal' padahal nginx MENJAWAB dan container sudah healthy + install sukses (det-1/enh-1), dan konklusivitas BO dikunci per-AREA sehingga fatal BO membuat login gagal yang lalu melucuti expect_no_fatal yang ada persis untuk menangkap fatal itu (enh-2, pass=true atas BO mati). Arah kedua: infra murni dijual sebagai vonis memblok karena PSM_INSTALL_FAIL ditulis tapi tak pernah dibaca, jadi 'docker exec mati' dan 'installer menolak module' jatuh ke jawaban yang sama (enh-3). Aturan yang hilang: kanal infra hanya sah untuk kondisi SEBELUM prasyarat vonis lengkap.
- Fix: Buat probe & parser mengembalikan FAKTA, bukan SEBAB. wait_http kembalikan (responded, status, transport_error): infra hanya bila TAK ada respons TCP; respons HTTP berapa pun -> lanjut ke browser, biarkan expect_no_fatal memvonis. Konklusivitas BO diganti per-AKSI (aksi yang menilai respons server konklusif tanpa auth; hanya yang bergantung isi ber-auth yang butuh sesi), diturunkan dari SUPPORTED_ACTIONS satu tempat. parse_install baca PSM_INSTALL_FAIL -> installer_ran; tak tercapai = infra. Ketiganya wajib difix berpasangan + test yang terbukti bisa gagal (mutasi copot gerbang reachability sekarang HIJAU = nol coverage).
- Findings:
  - `determinism-1` Probe reachability Lapis 4 mengklasifikasi FO HTTP 500 sebagai INFRA — menelan 2 temuan konklusif yang memblok — `scripts/ps-e2e-run.py:589-604 (wait_http) + :821-824 (call site)`
  - `enhancement-1` REMOVE: gerbang reachability yang mengklasifikasi HTTP 500 sebagai infra (vonis atas temuan run container nyata) — `scripts/ps-e2e-run.py:589-604 (wait_http) + :821-823 (run_one_version)`
  - `enhancement-2` REMOVE: gerbang konklusivitas BO per-AREA — fatal BO melucuti assertion yang seharusnya menangkapnya (pass=true atas toko rusak) — `scripts/ps-e2e-run.py:414 (conclusive = (area != "bo") or ctx.get("bo_authed")) + :621-652 (_bo_login)`
  - `enhancement-3` ADD: pembaca untuk PSM_INSTALL_FAIL — tanpanya kegagalan infra dijual sebagai vonis memblok 'modul gagal install di core asli' — `scripts/ps-flashlight-run.py:78-88 (INSTALL_BLOCK_SH) + :192-203 (parse_install) + scripts/ps-aggregate.py:278-286`

### 2. Gerbang ada dan benar di satu seam, kembarannya telanjang

- Root cause: Persis kelas yang dinamai kedua verifier ronde-5, dan sapuan mekanis ronde ini menemukan lima anggota lagi. Gerbang TIPE cakupan dijaga di static.rules_evaluated & e2e.authored_assertions (mutasi keduanya merah) tapi flashlight.coverage_ok cuma diuji NILAI lalu KEBERADAAN — celah di antaranya: null/'false'/0 memulihkan ready=true (det-2). Gerbang anti-drift mengunci 5 key tapi psm_flashlight_orchestrator tak punya konstanta sama sekali (cust-1), dan hardcode per-key berarti key ke-11 mana pun lahir tak berpenjaga. reject_unresolved_paths ada 3x di psm-setup, 0x di psm-validate yang justru konsumen terberat (cust-3). Salinan keempat tag map hidup di prosa quickstart tanpa penjaga, padahal gerbang doc-vs-konstanta sudah ada untuk string lain (cust-4). Dan jalur fallback 'resolver absen -> default kanonik skrip' tak punya default untuk psm_reports_dir (cust-2).
- Fix: Berhenti menambal per-situs; jadikan tiap gerbang pemilik-tunggal lalu pasang di SEMUA seam sebentuk. coverage_fact(container,key) -> True|False|None dipakai ketiga seam cakupan. Gerbang drift diubah dari daftar hardcode jadi ENUMERASI (tiap key psm_flashlight_* wajib punya DEFAULT_<SUFFIX> padanan; terbukti perlu: key bogus baru lolos semua suite). reject_unresolved_paths diangkat jadi milik bersama & dipanggil di tiap seam penerima path config, exit 2 selaras kontrak. Tag di doc diturunkan dari konstanta atau dijaga gerbang doc yang sudah ada.
- Findings:
  - `determinism-2` Gerbang TIPE cakupan telanjang di seam flashlight: coverage_ok null|"false"|0 memulihkan ready=true — `scripts/ps-aggregate.py:324-336 (flashlight_layer)`
  - `customization-1` psm_flashlight_orchestrator: satu-satunya key ber-salinan-skrip yang TAK dijaga test anti-drift (5 saudaranya dijaga) — `scripts/tests/test-ps-flashlight-run.py:277-294 (gerbang drift); ps-flashlight-run.py:517 + ps-e2e-run.py:905 (literal telanjang)`
  - `customization-2` Jalur fallback 'resolver absen -> default kanonik skrip' TAK BISA DIJALANKAN untuk psm_reports_dir — `SKILL.md:19 (janji fallback) vs scripts/ps-plan-layers.py:191 (required=True, tanpa default)`
  - `customization-3` reject_unresolved_paths: kelas BELUM tertutup di konsumen — 3x di psm-setup, 0x di psm-validate yang konsumen terberat — `scripts/ps-plan-layers.py (menelan token, exit 0); bandingkan psm-setup/scripts/{cleanup-legacy.py:261, merge-config.py:499, merge-help-csv.py:142}`
  - `customization-4` Salinan KEEMPAT tag map hidup di prosa quickstart tanpa penjaga — padahal gerbang doc-vs-konstanta sudah ada untuk string lain — `references/e2e-quickstart.md:25,116 vs tests/test-ps-flashlight-run.py:256-258 (gerbang doc yang SUDAH ada)`

### 3. Sinyal diemit tanpa pembaca — termasuk fitur resume yang membeli NOL

- Root cause: Kelas lama yang belum habis, dan kali ini korbannya sebuah fitur utuh. plan.reuse & plan.path diproduksi ps-plan-layers tapi SKILL.md cuma merutekan `rerun`, lalu §Vonis menyuruh 'lewati flag lapis yang tak dijalankan' — jadi lapis yang plan bilang sah dipakai-ulang dibuang di gerbang: ready=false atas bukti yang ADA dan segar, terbaca palsu sebagai 'jujur, runner tak sanggup' (arch-2). e2e_scenario_notes memperingatkan spec rusak SEBELUM boot mahal tapi tak dirutekan (arch-4). Penyempitan browser wajib dilaporkan SKILL.md:39 tapi tak punya field di agregat — model melaporkannya dari ingatan percakapan, dan pemanggil headless tak bisa melihatnya sama sekali (det-4). Sebaliknya e2e_scenario_sources & plan.e2e_scenarios adalah echo tanpa pembaca yang justru menawarkan ulang premis struktural yang skill ini sudah bayar mahal untuk buang (enh-4/enh-5).
- Fix: Untuk tiap field: rutekan atau hapus, jangan biarkan menganggur. Rutekan — §Vonis konsumsi 'tiap lapis yang PUNYA file termasuk yang ditandai reuse' (netral token, memulihkan fitur resume); e2e_scenario_notes disebut di baris plan yang sudah ada; e2e_browsers/e2e_browsers_run di-echo agregat memakai pola echo yang sudah ada di sebelahnya. Hapus — e2e_scenario_sources & plan.e2e_scenarios. DITOLAK dgn bukti (jangan dijadwalkan lagi): 'nama bentrok' e2e_scenario_notes (satu pemilik, makna sama = justru benar) dan echo diagnostik bernilai rendah.
- Findings:
  - `architecture-2` Mode resume: reuse:true + path diproduksi plan tapi TAK PERNAH dikonsumsi — instruksi agregat membuang bukti yang dipakai-ulang, ready jatuh diam-diam — `SKILL.md:25 (§Validate) -> SKILL.md:37 (§Vonis dan output)`
  - `architecture-4` e2e_scenarios/e2e_scenario_notes dari plan diemit ke ruang hampa — peringatan pra-boot yang tak dirutekan — `scripts/ps-plan-layers.py:247-250 -> SKILL.md:25`
  - `determinism-4` Penyempitan cakupan browser tak punya field di laporan — SKILL.md menyuruh model melaporkannya dari ingatan percakapan — `scripts/ps-aggregate.py:729-765 (result) + SKILL.md:39; produsen scripts/ps-e2e-run.py:957`
  - `enhancement-4` REMOVE: e2e_scenario_sources di agregat — echo tanpa pembaca yang menawarkan ulang premis struktural yang sudah didiskreditkan — `scripts/ps-aggregate.py:750-755`
  - `enhancement-5` REMOVE: plan.e2e_scenarios (echo yatim); sisa inventaris echo + 'nama bentrok' dinilai DERAU — jangan dibuka lagi — `scripts/ps-plan-layers.py:167-185, :247-250`

### 4. Janji SKILL.md ke artefak yang tak bisa memenuhinya

- Root cause: Prosa menamai artefak yang bentuknya salah, lalu cabangnya menyala tiap run tanpa pernah bisa menghasilkan apa pun. Step 3 menjanjikan aturan pindai dari tech/validator-rules.md — file itu PROSA yang baris ketiganya justru merujuk balik ke assets/ps-rules.json ('file ini untuk gambaran'), dan meneruskannya persis seperti SKILL suruh = exit 2; kanal keduanya (tag dari flashlight.md) byte-identik dgn config = no-op (arch-3). Hop e2e-quickstart -> adversarial-lens.md (temuan ronde-5) MASIH tak resolve, tepat di titik model harus menulis cacat visual yang memblok (arch-1). ambiguous_main_file menyuruh model menurunkan ulang _extends_module — kerja yang skrip sudah jalankan & buktikan tak berjawab tunggal, atas fakta yang skrip punya lalu buang, dan jawabannya tak punya kanal untuk dipakai (det-3). Kata 'MENAMBAH' untuk --extra-tag-map padahal ia MENAMBAL: memori KB berbulan-bulan diam-diam mengalahkan config yang baru diset user (cust-5).
- Fix: Cabut nama file .md dari janji-mesin (bebaskan ~60 token yang membayar fix prosa lain); perbaiki hop jadi references/adversarial-lens.md dan tutup konvensi gandanya; emitkan extends_module ke main_file_candidates lalu trim prosanya (justru MENGURANGI token); ganti 'MENAMBAH' -> 'MENAMBAL (yang tambahan menang)' — netral biaya.
- Findings:
  - `architecture-3` Cabang augmentasi KB (On Activation step 3) mati secara struktural di KEDUA kanal — menyala tiap run, tak pernah bisa menghasilkan apa pun — `SKILL.md:21 (§On Activation step 3)`
  - `architecture-1` Hop e2e-quickstart.md -> adversarial-lens.md tak resolve (temuan ronde-5 MASIH hidup) + nesting reference->reference — `references/e2e-quickstart.md:165`
  - `determinism-3` ambiguous_main_file menyuruh model menurunkan ulang _extends_module — kerja yang skrip sudah jalankan & buktikan tak berjawab tunggal — `SKILL.md:27 (Lapis 1) + scripts/ps-static-scan.py:66-91 (find_main_file)`
  - `customization-5` --extra-tag-map dari knowledge base MENGGANTI tag config untuk versi yang bertabrakan, sementara SKILL.md menyebutnya 'MENAMBAH' — `SKILL.md:21 vs scripts/ps-flashlight-run.py:168-178 (parse_tag_map)`

### 5. Leanness rebound: satu aturan, lima pemilik

- Root cause: Pola yang persis diprediksi — tiap fix pass menambahkan penjaga di situsnya sendiri alih-alih memperkuat pemilik tunggal, sampai 'jangan nilai ulang' berdiri di lima situs (209 token) sementara doktrin 'satu aturan satu pemilik' sudah lama berlaku di lapis skrip (CONTAINER_PREFIX, is_skipped, version_ready) — prosanya belum ikut. Prasyarat playwright adalah salinan KEEMPAT dari perintah yang skrip sudah emitkan tepat saat dibutuhkan, dengan engine yang tepat. Aturan port-leak diduplikasi di 2 file dan sudah MENYIMPANG: salinan SKILL.md terbukti membocorkan network di Docker nyata — dan .memlog.md:70 mengklaim sebaliknya. SKILL.md 2995/3000, headroom 5, jadi trim ini yang membayar fix tema lain.
- Fix: Kerjakan trim LEBIH DULU supaya fix lain punya ruang: angkat 'jangan nilai ulang' ke satu pemilik di lead Validate sambil pertahankan tiap fakta spesifik-situs (-32), buang perintah playwright inline & sisakan rutenya (-16), samakan perintah port-leak ke versi lengkap yang terbukti benar (+24, dibayar) lalu jadikan quickstart merujuk balik. Paket bersih 2995 -> 2936. Koreksi juga klaim .memlog.md:70 agar ronde depan tak mewarisi kesimpulan yang sudah direfutasi.
- Findings:
  - `leanness-1` 'Jangan nilai ulang' di-restate di 5 situs — satu aturan, lima pemilik — `SKILL.md:25,27,29,33,37 (Validate + Vonis)`
  - `leanness-2` Prasyarat playwright install di SKILL.md = salinan ke-4 dari yang sudah diemit skrip tepat saat dibutuhkan — `SKILL.md:33 (Lapis 4)`
  - `leanness-3` Aturan pembersihan port-leak diduplikasi di 2 file dan sudah MENYIMPANG — salinan SKILL.md terbukti membocorkan network — `SKILL.md:29 (Lapis 2) vs references/e2e-quickstart.md:170-172`
  - `leanness-4` Overview menarasikan ulang keempat lapis — salinan ke-3 setelah frontmatter dan header Lapis — `SKILL.md:10 (Overview)`
  - `architecture-5` 6.6 MB output analisis builder ter-track di dalam folder skill — `.analysis/ (82 file ter-track)`

## Strengths

- NOL dead wire, ronde kedua berturut: kesepuluh key psm_* diverifikasi sampai titik pakai lewat grep per-key + resolver dijalankan; resolver TERKONFIRMASI mekanisme config satu-satunya (tak ada customize.toml, env-var, settings, atau file config yang di-author skill).
- Empat invarian false-pass utuh by construction; degrade jujur & delegasi vonis ke skrip bertahan di seluruh sapuan.
- Gerbang CI headless BISA dipenuhi — temuan 'permanen merah' ronde-5 mati: default --require di runner tanpa Docker -> ready=False pass=True, escape hatch --require static -> ready=True. Panduan SKILL.md akurat.
- Kontrak exit-2 utuh & konsisten: --rules prosa, --extra-rules prosa, --extra-rules path tak ada — semuanya exit 2 persis seperti dijanjikan.
- Gotcha port-leak TIDAK lagi drift di sisi kode: CONTAINER_PREFIX satu prefix untuk kedua orkestrator; ps-e2e-run mengimpor orkestrasi flashlight by-path, jadi tak ada implementasi kedua yang bisa mendrift.
- Gerbang tipe cakupan di static.rules_evaluated & e2e.authored_assertions terbukti NYATA dan terjaga test — mutasi keduanya merah. Ia jadi baseline yang menelanjangi seam ketiga.
- Fix ronde-4 (ekspansi {project-root} di titik emit resolver) terverifikasi masih tertutup di jalur emit; discover_scenarios tetap pemilik tunggal aturan spec, dipakai plan & agregat tanpa duplikasi.

## Recommendations

1. Tutup kesalahan kanal di ketiga seam sekaligus (CRITICAL). wait_http kembalikan fakta bukan sebab -> infra hanya bila tak ada respons TCP; konklusivitas BO per-AKSI bukan per-AREA; parse_install baca PSM_INSTALL_FAIL. Wajib berpasangan — fix satu situs berarti kelasnya selamat satu seam di sebelahnya untuk ronde ke-6. Tiap fix butuh test yang terbukti bisa gagal: jalur reach_ok=False sekarang NOL coverage (mutasi copot gerbang = suite tetap hijau), dan test:271 justru MEMBEKUKAN cacat BO. (resolves: determinism-1, enhancement-1, enhancement-2, enhancement-3)
2. Trim dulu, sebelum menyentuh apa pun yang menambah prosa — headroom 5 token. Paket leanness (-48 bersih) + cabut step-3 mati (-60) membiayai fix prosa di rank 3-4. Perbaiki sekalian klaim .memlog.md:70 yang sudah direfutasi Docker nyata. (resolves: leanness-1, leanness-2, leanness-3, leanness-4, architecture-3)
3. Satu pemilik untuk tiap gerbang, dipasang di semua seam sebentuk: coverage_fact() di ketiga seam cakupan; gerbang drift dari hardcode jadi enumerasi; reject_unresolved_paths jadi milik bersama di tiap penerima path config; default kanonik untuk psm_reports_dir; tag doc dijaga gerbang yang sudah ada. (resolves: determinism-2, customization-1, customization-2, customization-3, customization-4)
4. Rutekan atau hapus tiap sinyal yang menganggur — prioritaskan arch-2: fitur resume sekarang membeli NOL dan vonisnya berlawanan atas bukti identik. Perbaikannya netral token. (resolves: architecture-2, architecture-4, determinism-4, enhancement-4, enhancement-5)
5. Benahi janji yang tak bisa ditepati: hop reference, extends_module di-emit lalu prosanya ditrim (mengurangi token), 'MENAMBAH' -> 'MENAMBAL'. (resolves: architecture-1, determinism-3, customization-5)
6. Kebersihan distribusi: .analysis/ (6.6 MB, 82 file ter-track) masuk .gitignore + git rm --cached; riwayat tetap di git. Jangan sentuh .memlog.md — ia memori resume yang aktif. (resolves: architecture-5)

## Experience

- **Validasi interaktif module (4 lapis)** — On Activation resolve config -> tawarkan cakupan versi/browser -> Lapis 1 statis -> Lapis 2 flashlight -> Lapis 3 adversarial -> Lapis 4 E2E browser -> ps-aggregate rakit vonis. Cacat rank-1 menyerang di Lapis 4: module yang merusak storefront/BO dilaporkan sebagai kegagalan Docker, bukan sebagai cacat module.
- **Resume run terputus** — ps-plan-layers menandai reuse/rerun -> model jalankan yang rerun -> agregat. RUSAK (arch-2): §Vonis membuang file lapis yang ditandai reuse, jadi ready jatuh atas bukti yang ada & segar; fitur resume membeli nol.
- **Headless / CI** — Sehat & terverifikasi: kontrak JSON status complete|blocked, gerbang atas pass DAN konklusivitas, --require sbg escape hatch yang terbukti bisa dipenuhi di runner tanpa Docker, memlog init+append.
- Headless: Terverifikasi bisa dipenuhi lewat reproduksi (bukan klaim): default ketat -> ready=False pass=True jujur; --require static -> ready=True. Satu lubang tersisa: penyempitan browser tak punya field, jadi pemanggil headless (psm-develop/scaffold/cross-version) tak bisa melihat cakupan yang dipersempit sama sekali.

## Findings

### Critical (1)

#### determinism-1 — Probe reachability Lapis 4 mengklasifikasi FO HTTP 500 sebagai INFRA — menelan 2 temuan konklusif yang memblok

- Lens: determinism
- Location: `scripts/ps-e2e-run.py:589-604 (wait_http) + :821-824 (call site)`
- Evidence: Di call site dua prasyarat sudah tegak: wait_healthy ok dan install.ok True (return dini bila tidak). Reproduksi dgn server HTTP nyata yang membalas 500: wait_http -> ok=False status='HTTP 500' -> errors.append('PS tak terjangkau — port publish/boot gagal') -> return SEBELUM browser diluncurkan; ps-aggregate.e2e_layer -> not_conclusive, errors=0, NOL temuan memblok. Body 500 yang SAMA diberi ke browser: goto ok=False conclusive=True; expect_no_fatal ok=False conclusive=True -> 2 temuan KONKLUSIF severity=error. Jadi kanal infra menelan tepat 2 temuan konklusif. Alasan yang sampai ke manusia berkebalikan dgn fakta: nginx MENJAWAB (500 well-formed) = port & boot terbukti bekerja. Informasinya ADA lalu dibuang — wait_http membedakan 'HTTP {code}' (server hidup) dari str(e) (tak ada respons TCP), tapi call site cuma baca bool. Jalur ini NOL coverage: MUTASI copot SELURUH gerbang (:821-824) -> test-ps-aggregate GREEN, test-ps-e2e-run GREEN. wait_http hanya pernah di-stub lambda: (True, 200).
- Recommendation: Intelligence leak: skrip menyimpulkan SEBAB (infra) dari gejala, di titik ia sendiri sudah membuktikan sebab itu mustahil. wait_http kembalikan (responded, status, transport_error); gerbang infra hanya menyala bila responded False. Respons HTTP berapa pun -> jangan return, lanjut ke browser, biarkan expect_no_fatal memvonis. Aturan kelasnya: kanal infra hanya sah untuk kondisi SEBELUM prasyarat vonis lengkap (wait_healthy timeout & install gagal tetap sah). Tambah test CLI atas cabang reach_ok=False sampai mutasi 3 merah.

### High (7)

#### enhancement-1 — REMOVE: gerbang reachability yang mengklasifikasi HTTP 500 sebagai infra (vonis atas temuan run container nyata)

- Lens: enhancement
- Location: `scripts/ps-e2e-run.py:589-604 (wait_http) + :821-823 (run_one_version)`
- Evidence: VONIS: YA, kanal infra menelan temuan konklusif. Direproduksi dgn server lokal 500 + body fatal PHP: wait_http -> (False,'HTTP 500') setelah 6.2s poll -> errors += 'port publish/boot gagal', findings=[] (browser tak pernah diluncurkan). Body yang sama bila browser jalan: fatal_sign_in -> 'Fatal error'; _fatal_message(500,body) -> 'HTTP 500 (server error)' -> temuan konklusif memblok. wait_http SUDAH membedakan 'HTTP 500' (server menjawab) vs OSError (infra sejati) tapi pemanggil meratakannya. Konteks menutup celah tafsir: probe berjalan SESUDAH wait_healthy lolos dan SESUDAH install sukses via bin/console (boot kernel PS penuh), dan 500 bertahan sepanjang startup_timeout (500 transien akan ter-resolve jadi ok=True oleh loop poll). Efek: operator diberi tahu Docker-nya rusak atas module yang mem-FATAL storefront — kebalikan dari alasan Lapis 4 ada; plus nol screenshot & nol tanda fatal, bukti terbaik dibuang saat paling dibutuhkan.
- Recommendation: Turunkan probe jadi 'apakah ada yang mendengarkan?' dan serahkan penilaian ke pihak yang punya bukti (browser). wait_http -> (ok, status, responded); bila responded tapi not reach_ok: JANGAN return dini, JANGAN tulis ke errors — lanjut ke browser, smoke universal memvonis lengkap dgn tanda fatal + screenshot. Hanya bila tak ada respons sama sekali barulah errors + pesan 'port publish/boot gagal' jujur. Seam kembar wait_http kedua (:830 warm-up BO) sudah membuang hasilnya — aman.

#### enhancement-2 — REMOVE: gerbang konklusivitas BO per-AREA — fatal BO melucuti assertion yang seharusnya menangkapnya (pass=true atas toko rusak)

- Lens: enhancement
- Location: `scripts/ps-e2e-run.py:414 (conclusive = (area != "bo") or ctx.get("bo_authed")) + :621-652 (_bo_login)`
- Evidence: SEAM KEMBAR dari enhancement-1, ditemukan lewat 'seam mana lagi punya cacat ini?'. Reproduksi dgn FO SEHAT (200) + BO 500 berisi 'Fatal error: Uncaught Error: Call to undefined method Psm::getTabs()' — module yang merusak BACK OFFICE saja, jadi enhancement-1 tak menutupinya: bo_authed=False; findings (memblok) = []; inconclusive = [goto status=500, expect_no_fatal 'HTTP 500 (server error)']; pass untuk versi ini = True -> exit 0 -> Lapis 4 melaporkan HIJAU atas module yang mem-fatal BO. Lingkarannya tertutup: fatal BO -> _bo_login gagal -> bo_authed=False -> SETIAP assertion BO tak konklusif -> termasuk expect_no_fatal yang ada persis untuk menangkap fatal itu. ready memang tetap jatuh, tapi lewat kanal & pesan yang salah ('2 assertion tak konklusif (mis. login BO gagal)') — operator membaca 'kredensial admin salah', bukan 'BO-mu mati'. Premis gerbang benar tapi butirannya terlalu kasar: pada BO PS sehat, request tanpa auth me-redirect ke AdminLogin (200/302), jadi expect_no_fatal & status<500 lolos tanpa login. Test:271 MEMBEKUKAN cacat ini ('BO expect_no_fatal TAK konklusif tanpa login').
- Recommendation: Ganti gerbang per-area jadi per-AKSI: himpunan aksi yang butuh sesi (expect_visible, expect_text, click, fill) vs yang tidak (goto/status, expect_no_fatal, expect_no_console_error — menilai respons server, bukan isi ber-auth). conclusive = (area != 'bo') or bo_authed or action not in AUTH_DEPENDENT_ACTIONS, diturunkan dari SUPPORTED_ACTIONS di satu tempat (pola ASSERT_ACTIONS). Persempit test:271 ke expect_visible + TAMBAH assert yang merah sebelum fix. Yang hilang: tak ada — tak satu pun halaman BO PS sehat mengembalikan 5xx/fatal ke request tanpa auth.

#### enhancement-3 — ADD: pembaca untuk PSM_INSTALL_FAIL — tanpanya kegagalan infra dijual sebagai vonis memblok 'modul gagal install di core asli'

- Lens: enhancement
- Location: `scripts/ps-flashlight-run.py:78-88 (INSTALL_BLOCK_SH) + :192-203 (parse_install) + scripts/ps-aggregate.py:278-286`
- Evidence: CERMIN enhancement-1 (arah berlawanan, kelas sama). Bukti orphan: 'PSM_INSTALL_FAIL disebut parse_install? -> False'; parse_install('...PSM_INSTALL_FAIL...') / parse_install('kalimat acak') / parse_install('') -> ketiganya {'ok': False}. Tiga input yang MAKNANYA BERBEDA jatuh ke jawaban sama. MUTASI (echo PSM_INSTALL_FAIL -> PSM_BANANA): 'SEMUA TEST LOLOS' — assert test:77 lolos karena PSM_INSTALL_OK ABSEN, bukan karena PSM_INSTALL_FAIL ADA (aturan-main #2). Akibat, direproduksi untuk docker exec yang gagal sendiri sesudah healthy (_exec:410 tak pernah memeriksa cp.returncode): parse_install('Error response from daemon: Container 9f2a is not running') -> flashlight_layer conclusive=True state=fail -> TEMUAN MEMBLOK 'modul gagal install di core asli'. Infra murni -> vonis konklusif memblok. Docstring parse_install menulis sendiri aturannya ('sentinel yang dibaca tapi tak pernah ditulis = jalur degrade mati, infra jatuh jadi vonis memblok palsu') lalu melakukan kebalikannya. Melanggar janji inti dari sisi sebaliknya: 'klaim GAGAL konklusif atas yang tak diuji'.
- Recommendation: Baca sentinel yang sudah ada: installer_ran = ('PSM_INSTALL_OK' in out) or ('PSM_INSTALL_FAIL' in out); kembalikan no_verdict = not installer_ran. Di flashlight_layer, ikutkan ke daftar infra bersama no_console/no_psroot -> not_conclusive, tak memblok. Vonis memblok lalu hanya lahir dari PSM_INSTALL_FAIL yang benar-benar diemit = bukti PS console MENJALANKAN installer dan menolaknya. Test mutasi-sensitif: parse_install('teks acak') -> tak konklusif SEDANGKAN parse_install(PSM_INSTALL_FAIL) -> konklusif memblok. Seam kembar ps-e2e-run.install_module aman by design (Lapis 2 yang memvonis); cukup teruskan field barunya agar pesan :812-817 jujur.

#### architecture-1 — Hop e2e-quickstart.md -> adversarial-lens.md tak resolve (temuan ronde-5 MASIH hidup) + nesting reference->reference

- Lens: architecture
- Location: `references/e2e-quickstart.md:165`
- Evidence: Baris 165: 'Bentuk lengkap + aturan yang ditegakkan (pelanggaran = exit 2): `adversarial-lens.md`.' Konvensi file itu sendiri menutup tafsir — baris 8: 'Semua path di bawah relatif ke folder skill', dan §Resolution rules SKILL.md senada. Jadi bare path -> {skill-root}/adversarial-lens.md. Reproduksi: `ls -la adversarial-lens.md` -> 'No such file or directory'; file nyata di references/adversarial-lens.md. Sapuan penuh hop di references/ menunjukkan ini SATU-SATUNYA hop .md antar-file, jadi juga satu-satunya pelanggaran canon test 6 (references stay one level deep). Titik gagalnya justru yang paling mahal: model membacanya tepat saat harus menulis cacat visual yang MEMBLOK ke file adversarial — satu-satunya kanal judgment model ke vonis.
- Recommendation: Ubah ke references/adversarial-lens.md. Kelasnya lahir dari konvensi ganda — baris 8 menyatakan path relatif ke folder skill sementara penulis menulis relatif ke folder file: hapus baris 8 dan andalkan §Resolution rules, ATAU pastikan tiap path di file ini ber-prefix folder. Karena nesting reference->reference dilarang canon, alternatif lebih bersih: ganti hop jadi rujukan otoritatif yang file ini sudah pakai di baris 112 (`ps-aggregate.py --help`) dan biarkan SKILL.md tetap satu-satunya yang merutekan references/adversarial-lens.md.

#### architecture-2 — Mode resume: reuse:true + path diproduksi plan tapi TAK PERNAH dikonsumsi — instruksi agregat membuang bukti yang dipakai-ulang, ready jatuh diam-diam

- Lens: architecture
- Location: `SKILL.md:25 (§Validate) -> SKILL.md:37 (§Vonis dan output)`
- Evidence: SKILL.md:25 merutekan resume ('jalankan lapis yang ditandainya rerun'); SKILL.md:37 lalu berkata 'lewati flag lapis yang tak dijalankan'. Di mode resume keduanya bertabrakan: lapis ber-reuse:true TAK dijalankan sesi ini, tapi filenya sah. Reproduksi: ps-plan-layers -> static reuse=True, adversarial reuse=True, rerun=['flashlight','e2e']. Model mengikuti SKILL:37 apa adanya -> ps-aggregate --static ... --require static,adversarial -> ready=False, layers_run adversarial=False. Menyodorkan file yang dipakai-ulang -> ready=True, adversarial=True. Dua vonis BERLAWANAN atas bukti identik, ditentukan semata oleh tafsir 'dijalankan'. Kelas sinyal-yatim: plan.layers.<l>.path & .reuse diemit dgn pemanggil NOL. Fitur resume membeli NOL, dan ready:false-nya terbaca 'jujur, runner tak sanggup' padahal buktinya ADA dan segar.
- Recommendation: Buat §Vonis mengonsumsi apa yang plan produksi: ganti '(lewati flag lapis yang tak dijalankan)' jadi '(sertakan tiap lapis yang PUNYA file — termasuk yang plan tandai reuse; lewati hanya lapis tanpa file)'. Netral token, menutup ambiguitas 'dijalankan' (sesi ini vs pernah). Sekelas dgn architecture-4 — perbaiki rute output plan sekali.

#### architecture-3 — Cabang augmentasi KB (On Activation step 3) mati secara struktural di KEDUA kanal — menyala tiap run, tak pernah bisa menghasilkan apa pun

- Lens: architecture
- Location: `SKILL.md:21 (§On Activation step 3)`
- Evidence: Kedua file KB ADA, jadi cabang menyala SETIAP run di project ini. (a) --extra-rules: validator-rules.md adalah PROSA, dan baris 3-nya menyatakan dirinya turunan ruleset inti ('Ruleset lengkap (regex per rule) ada di ps-rules.json — rujuk ke sana untuk deteksi, file ini untuk gambaran'). Meneruskannya seperti SKILL suruh: ps-static-scan --extra-rules validator-rules.md -> 'error: gagal baca extra-rules: Expecting value: line 1 column 1' REAL EXIT=2. (b) --extra-tag-map: tag map di flashlight.md:11 BYTE-IDENTIK dgn psm_flashlight_tag_map yang sudah dimuat step 1 -> no-op duplikat. Ini KELAS, bukan situs: SKILL menamai file memori KB — yang menurut desainnya catatan prosa manusia — sebagai sumber artefak-mesin. Kontrak exit-2 sendiri terbukti benar & konsisten, jadi cacatnya murni janji SKILL yang salah menamai artefak.
- Recommendation: Cabut nama file .md dari janji-mesin; namai artefak JSON/peta eksplisit saja ('--extra-rules <file.json>' Lapis 1, '--extra-tag-map <versi=tag>' Lapis 2/4; keduanya MENAMBAL, --rules/--tag-map MENGGANTI; tanpa artefak itu lanjut — aturan inti sudah di-embed). Menghapus dua path KB + klausa 'baca' MEMBEBASKAN ~60 token yang membayar architecture-2 & architecture-4. Bila memang ada aturan KB belum ter-embed, jalur benarnya menuliskannya ke assets/ps-rules.json — bukan berharap model menerjemahkan prosa jadi rule saat run, yang justru dilarang SKILL.md:21 sendiri.

#### determinism-2 — Gerbang TIPE cakupan telanjang di seam flashlight: coverage_ok null|"false"|0 memulihkan ready=true

- Lens: determinism
- Location: `scripts/ps-aggregate.py:324-336 (flashlight_layer)`
- Evidence: Payload dirakit tangan, versi 9.1, keempat lapis bersih: cov-true -> ready=True; cov-false -> ready=False (+note); cov-null -> ready=True BOCOR; cov-str('false') -> ready=True BOCOR; cov-zero(0) -> ready=True BOCOR; cov-absent -> ready=False (+note skrip lama). Seam kembar A (static.rules_evaluated) & B (e2e.authored_assertions) menolak SETIAP nilai salah-tipe (null/'0'/true/-1 -> False). Penyebab: cabang flashlight cuma menguji NILAI (cs.get('coverage_ok') is False) lalu KEBERADAAN ('coverage_ok' not in cs) — celah persis di antaranya; bandingkan static_layer:197 dan authored_assertions:356 yang memakai isinstance. MUTASI 1 (copot gerbang tipe rules_evaluated) -> RED; MUTASI 2 (authored_assertions) -> RED; di seam flashlight tak ada apa pun untuk dimutasi. Test coverage_ok cuma menyentuh True/False/absen — nol kasus salah-tipe. Alasan yang ditulis dua seam kembar itu sendiri ('file lapis dari skrip lama atau salah bentuk') berlaku identik di sini: ps-plan-layers memang ADA untuk memakai-ulang file lapis lintas run.
- Recommendation: Determinisme terbalik: skrip memperlakukan nilai tak diketahui sebagai bukti teruji. Fix KELAS: ekstrak coverage_fact(container, key) -> True|False|None dan pasang di KETIGA seam (static.rules_evaluated, e2e.authored_assertions, flashlight.coding_standard.coverage_ok) supaya seam keempat yang lahir nanti tak bisa lupa. Konkretnya untuk flashlight: ganti rantai is False / not in dengan `if cs.get('coverage_ok') is not True:` + bedakan pesan absen vs salah-tipe. Tambah test salah-tipe di ketiga seam.

### Medium (11)

#### leanness-1 — 'Jangan nilai ulang' di-restate di 5 situs — satu aturan, lima pemilik

- Lens: leanness
- Location: `SKILL.md:25,27,29,33,37 (Validate + Vonis)`
- Evidence: grep -on mengembalikan varian aturan YANG SAMA di lima situs: L25 'jangan menilai kesegaran file dengan tangan', L27 'jangan menilai ulang temuannya dengan tangan', L29 'jangan nilai ulang', L33 'jangan nilai ulang outputnya', L37 'jangan rakit vonis dengan tangan atau menilai ulang/menimpa field'. Persis pola rebound: tiap ronde fix menambahkan penjaga baru di situsnya sendiri, bukan memperkuat pemilik tunggal. Bobot terukur (tiktoken): kelima klausa + lead = 209 token. GREP WIRE (per peringatan ronde-5): TIDAK ADA yang jadi sole wire — tiap situs mempertahankan fakta spesifiknya (rute ps-plan-layers; 'sumber kebenaran aturan yang diketahui pasti'; 'degrade sendiri lalu menandainya di output'; '--help'; 'Serahkan penggabungan ke skrip'). Yang dibuang hanya klausa perintahnya.
- Recommendation: Angkat ke SATU pemilik di lead Validate, strip klausa per-situs sambil MEMPERTAHANKAN tiap fakta spesifik-situs. Ini membayar leanness-3 (+24) dan menyisakan headroom. 'Satu aturan satu pemilik' sudah jadi doktrin di lapis skrip (CONTAINER_PREFIX, is_skipped, version_ready) — prosanya belum ikut.
- Proposed smallest: Lead Validate: 'Jalankan empat lapis pengujian lalu satukan jadi vonis — lapis murah dan deterministik dulu supaya run mahal tak diulang. Tiap skrip di bawah memiliki vonisnya sendiri: baca apa adanya, jangan nilai ulang, timpa, atau rakit ulang dengan tangan.' Lalu L25 -> '...dan jalankan lapis yang ditandainya rerun.'; L27 -> 'Ini sumber kebenaran untuk aturan yang diketahui pasti.'; L29 -> '...degrade sendiri lalu menandainya di output.'; L33 -> '...(format spec & degrade: --help).'; L37 -> 'Serahkan penggabungan ke skrip.'
- Predicted delta: Diprediksi TIDAK ADA yang hilang. Terukur: 209 -> 177 token (-32; SKILL.md 2995 -> 2963). Aturannya tetap dinyatakan sekali dgn cakupan lebih luas (menjangkau lapis mana pun termasuk yang ditambahkan nanti — kelima salinan justru mengundang lapis ke-5 yang lupa disalinkan penjaganya). Risiko diakui jujur: aturan ini menjaga kegagalan NYATA, jadi ini bukan penghapusan — ia tetap ada, sekali, di tempat yang dibaca sebelum semua lapis. Route ke variant eval: jalankan versi 5-situs vs 1-pemilik atas run yang sama, cek apakah model menilai ulang field skrip mana pun.

#### leanness-2 — Prasyarat playwright install di SKILL.md = salinan ke-4 dari yang sudah diemit skrip tepat saat dibutuhkan

- Lens: leanness
- Location: `SKILL.md:33 (Lapis 4)`
- Evidence: SKILL.md:33 memuat perintah inline lalu MERUTEKAN ke references/e2e-quickstart.md di tanda kurung yang sama. Salinan lain, diverifikasi dgn menjalankan skripnya: (1) ps-e2e-run.py --help baris 114 mencetak PERSIS 'Prasyarat browser (sekali, host): uv run --with playwright playwright install chromium firefox' — dan SKILL.md:33 sudah menyuruh membaca --help di kalimat yang sama; (2) skrip mengemitnya SAAT DIBUTUHKAN menamai engine yang hilang (L751/757/850/937); (3) e2e-quickstart.md:21 + L30-32 yang menjelaskan WHY (build browser harus cocok — salinan SKILL.md justru yang TANPA why-nya); (4) SKILL.md:43 sendiri. GREP WIRE: '<engine dari psm_e2e_browsers>' bukan sole wire (psm_e2e_browsers disebut SKILL.md:20 & e2e-quickstart.md:189); degrade jujur saat binary absen tetap utuh lewat browser_notes.
- Recommendation: Buang perintah inline, pertahankan rutenya. Kasus buku teks kanon 'mekanika yang tinggal di file yang salah' + 'mekanika untuk alat yang sudah didrive model dgn fasih': model tak perlu perintah ini SEBELUM menjalankan skrip — skrip yang memberitahunya, dgn engine yang tepat, tepat saat ia hilang.
- Proposed smallest: 'Prasyarat sekali (browser Playwright, setup mesin baru + loop TDD E2E): references/e2e-quickstart.md.'
- Predicted delta: Diprediksi TIDAK ADA yang hilang. Terukur: 48 -> 32 token (-16). Jalur mesin-baru tetap dilayani rute quickstart (versinya LEBIH LENGKAP — punya catatan pencocokan versi build yang salinan SKILL.md hilangkan); jalur binary-hilang tetap dilayani browser_notes/--help yang terbukti mencetak perintahnya.

#### leanness-3 — Aturan pembersihan port-leak diduplikasi di 2 file dan sudah MENYIMPANG — salinan SKILL.md terbukti membocorkan network

- Lens: leanness
- Location: `SKILL.md:29 (Lapis 2) vs references/e2e-quickstart.md:170-172`
- Evidence: Aturan sama ditulis dua kali dan kedua salinan sudah berbeda. SKILL.md:29: 'docker ps -aq --filter name=psm-fl | xargs -r docker rm -f' (container saja). e2e-quickstart.md:172: perintah itu PLUS 'docker network ls --filter name=psm-fl -q | xargs -r docker network rm'. Orkestrator manual memang membuat network (ps-flashlight-run.py:370-372) dan menghapusnya HANYA saat teardown bersih (:426-427) — yaitu justru bukan jalur 'di-kill paksa' yang gotcha ini bahas. DIREPRODUKSI dgn Docker nyata: dibuat psm-fl-net-leantest + container di network itu, dijalankan PERSIS perintah SKILL.md:29 -> 'SESUDAH container=0 network=1'; docker network ls masih menampilkan psm-fl-net-leantest. Perintah quickstart membersihkannya. CATATAN: .memlog.md:70 mengklaim 'satu filter name=psm-fl menangkap container DAN network (perintah gotcha SKILL.md terbukti benar)' — klaim itu SALAH untuk perintah sebagaimana tertulis; docker ps -aq tak pernah melisting network. GREP WIRE: SKILL.md:29 satu-satunya penyebut pembersihan ini di jalur Lapis 2 (quickstart hanya dirutekan dari Lapis 4), jadi ia TAK BOLEH dihapus — dua pemilik di sini adalah cacatnya.
- Recommendation: Satu pemilik, isi disamakan. Karena Lapis 2 tak pernah memuat quickstart, salinan SKILL.md TETAP ADA tapi diperbaiki agar cocok dgn yang lengkap (+24 token, DIBAYAR oleh -32 leanness-1); lalu ganti salinan quickstart:170-172 jadi rujukan balik satu baris, supaya penyimpangan berikutnya tak punya tempat tumbuh. Perbaiki juga klaim .memlog.md:70 agar ronde berikutnya tak mewarisi kesimpulan yang sudah direfutasi. Kelas: cek gotcha lain di quickstart yang punya kembaran ringkas di SKILL.md.

#### architecture-4 — e2e_scenarios/e2e_scenario_notes dari plan diemit ke ruang hampa — peringatan pra-boot yang tak dirutekan

- Lens: architecture
- Location: `scripts/ps-plan-layers.py:247-250 -> SKILL.md:25`
- Evidence: ps-plan-layers.py:241-246 menyatakan maksudnya sendiri: 'Validasinya murah, tapi dulu hanya dijalankan di dalam ps-e2e-run — SETELAH boot flashlight x N versi x M browser — jadi satu typo baru ketahuan setelah run mahal selesai.' Field-nya nyata & berisi (reproduksi): e2e_scenarios ['nogo.json']; e2e_scenario_notes ['broken.json: gagal parse — dilewati', 'nogo.json: tak ada aksi expect_* ...']. Tapi SKILL.md:25 merutekan SATU field saja ('jalankan lapis yang ditandainya rerun'). Nol instruksi membaca keduanya, jadi maksud yang dinyatakan source tak pernah terjadi. Prosa yang menjelaskan nilainya ADA tapi di tempat salah (e2e-quickstart.md:109,187 — hanya dimuat di cabang Lapis 4). Diperparah: pra-pass hanya dirutekan untuk resume, padahal validasi spec murah ini bernilai di SETIAP run. Verifikasi kepemilikan: klaim quickstart 'ps-plan-layers menandainya sebelum container boot' TERBUKTI BENAR (:182 memanggil e2e.discover_scenarios, satu pemilik). Diperiksa & BUKAN cacat: e2e_scenario_notes muncul di plan DAN agregat tapi keduanya dari discover_scenarios yang sama — nama sama, makna sama.
- Recommendation: Rutekan field yang sudah diemit & angkat pra-pass keluar dari cabang resume. Sisipkan ke kalimat plan yang sudah ada: '...dan jalankan lapis yang ditandainya rerun; bila ia menyebut e2e_scenario_notes, perbaiki spec itu SEBELUM Lapis 4 boot container (spec rusak/tanpa expect_* tak menaikkan ready).' (~+25 token, dibayar architecture-3). Kelas: e2e_scenario_sources (ps-aggregate.py:753-755) diemit tapi tak dirutekan — sudah di-supersede e2e_smoke_only, jadi DELETE yang benar.

#### determinism-3 — ambiguous_main_file menyuruh model menurunkan ulang _extends_module — kerja yang skrip sudah jalankan & buktikan tak berjawab tunggal

- Lens: determinism
- Location: `SKILL.md:27 (Lapis 1) + scripts/ps-static-scan.py:66-91 (find_main_file)`
- Evidence: SKILL.md:27: 'ambiguous_main_file bukan alasan berhenti: baca main_file_candidates, tentukan mana yang extends Module atau sebut folder perlu dinamai ulang.' Tapi skrip SUDAH menjalankan pertanyaan itu 20 baris di atas: ambiguous_main_file diproduksi HANYA saat len(extends) != 1 — yaitu 0 atau >=2 file extends Module. Jadi prompt menyuruh model menjawab pertanyaan yang skrip baru saja buktikan tak berjawab tunggal. Kedua kaki direproduksi: 2 file extends -> reason ambiguous_main_file, candidates ['legacy.php','mymod-master.php']; 0 file extends -> reason yang SAMA, candidates ['alpha.php','beta.php'] — cuma NAMA, tak ada info extends. Untuk mematuhi SKILL model harus MEMBUKA tiap .php dan meng-grep 'extends\s+Module': implementasi kedua dari helper yang sudah ada, dibayar token tiap kali, atas file source yang justru didelegasikan ke subagent di Lapis 3 supaya tak membebani konteks. Dan jawabannya buntu: --help -> tak ada --main-file.
- Recommendation: Determinism leak: 'tentukan mana yang extends Module' lolos ketiga pertanyaan uji determinisme — itu definisi kerja skrip, dan skripnya sudah ada. Terapkan pola pre-pass JSON: find_main_file sudah menghitung extends; emitkan faktanya alih-alih membuangnya — main_file_candidates: [{'file': 'mymod-master.php', 'extends_module': true}, ...]. Model lalu membaca JSON, bukan source, dan bisa membedakan dua kasus yang kini dilebur satu reason (0-extends = bukan module PrestaShop vs >=2-extends = butuh rename). Lalu TRIM prosa SKILL.md:27 — ini MENGURANGI token, jadi headroom 5 tak tersentuh.

#### determinism-4 — Penyempitan cakupan browser tak punya field di laporan — SKILL.md menyuruh model melaporkannya dari ingatan percakapan

- Lens: determinism
- Location: `scripts/ps-aggregate.py:729-765 (result) + SKILL.md:39; produsen scripts/ps-e2e-run.py:957`
- Evidence: SKILL.md:39 mewajibkan 'sebut eksplisit ... lapis yang tak jalan (layers_run), cakupan versi/browser yang dipersempit'. Tiap item lain menyebut fieldnya; 'browser' tidak — karena fieldnya tak ada. Reproduksi: top-level keys agregat tak memuat satu pun yang menyebut browser; e2e inconclusive_note '(none)'; sementara required_layers & target_versions MEREKAM penyempitan masing-masing. File lapis e2e MEMANG mencatatnya (browsers, browsers_available, :957) tapi keduanya top-level & dibaca NOL konsumen — ps-aggregate hanya membaca versions[].browsers untuk gerbang infra. Sinyal yatim di sisi produsen DAN lubang di sisi konsumen. browser_notes TIDAK menutup ini: ia hanya menyala saat engine diminta lalu gagal (probe_missed). Saat user SENGAJA memilih chromium saja di menu On Activation step 2, requested == usable -> nol note (dibuktikan: inconclusive_note '(none)' padahal firefox tak pernah dijalankan). Asimetri dgn keputusan yang sudah diambil: --require sengaja merekam pilihannya di required_layers justru supaya penyempitan 'bukan tersembunyi di flag yang kebetulan tak dipakai'.
- Recommendation: Determinism leak: membandingkan browser dikonfigurasi vs dijalankan berjawab-satu, tapi kini dibayar model tiap run — dan dibayar dari INGATAN percakapan, bukan laporan, sehingga psm-scaffold/psm-cross-version yang memanggil headless tak bisa melihatnya. Echo dua field yang produsennya SUDAH tulis ke top-level: e2e_browsers (diminta) & e2e_browsers_run (jalan), pola echo yang sama persis dgn e2e_screenshot_dir di sebelahnya, digerbang if e2e_ran yang sama. Nol prosa baru — kalimat :39 akhirnya punya field untuk dibaca. Kelas: browsers & browsers_available adalah anggota tersisa, sekelas versions[].image/install.no_console/no_psroot.

#### customization-1 — psm_flashlight_orchestrator: satu-satunya key ber-salinan-skrip yang TAK dijaga test anti-drift (5 saudaranya dijaga)

- Lens: customization
- Location: `scripts/tests/test-ps-flashlight-run.py:277-294 (gerbang drift); ps-flashlight-run.py:517 + ps-e2e-run.py:905 (literal telanjang)`
- Evidence: Test anti-drift mengunci per-key secara hardcode: DEFAULT_TAG_MAP, DEFAULT_DB_IMAGE, DEFAULT_PS_DOMAIN, DEFAULT_STARTUP_TIMEOUT, DEFAULT_BROWSERS, plus --versions di 4 skrip. psm_flashlight_orchestrator TIDAK ada di daftar itu — dan tak punya konstanta DEFAULT_ORCHESTRATOR sama sekali; nilainya literal telanjang default='auto' yang DISALIN di dua argparse. MUTASI (skrip -> 'compose', resolver tetap 'auto'): keenam suite hijau (test-ps-aggregate/e2e-run/flashlight-run/plan-layers/static-scan/resolve-psm-config semua failures:0). Bukan drift teoretis: komentar test-nya sendiri (:260-264) menyebut alasan kelima saudaranya WAJIB identik — SKILL.md merestui salinan skrip sbg jalur sah, dan drift semacam ini pernah ter-ship (tag 9.0=nightly & 8.1 usang) lalu ditemukan user, bukan CI. Orchestrator memikul rasional sama persis dan justru ia yang dilewat.
- Recommendation: Angkat ke DEFAULT_ORCHESTRATOR='auto' di ps-flashlight-run.py, pakai ulang di ps-e2e-run.py (default=fl.DEFAULT_ORCHESTRATOR, seperti --db-image sudah lakukan), tambah satu baris ke gerbang drift. Nol biaya token SKILL.md. FIX KELAS: gerbang drift hardcode per-key sehingga key ke-11 mana pun lahir tak berpenjaga — tutup dgn enumerasi (tiap key psm_flashlight_* di PSM_DEFAULTS wajib punya DEFAULT_<SUFFIX> padanan). Terbukti perlu: menambah psm_totally_bogus_key ke PSM_DEFAULTS membuat SEMUA suite tetap hijau.

#### customization-2 — Jalur fallback 'resolver absen -> default kanonik skrip' TAK BISA DIJALANKAN untuk psm_reports_dir

- Lens: customization
- Location: `SKILL.md:19 (janji fallback) vs scripts/ps-plan-layers.py:191 (required=True, tanpa default)`
- Evidence: SKILL.md:19 menjanjikan 'Resolver absen? Lanjut dengan default kanonik skrip dan catat bahwa default dipakai.' Grep konstanta kanonik yang benar-benar ADA: DEFAULT_BROWSERS/HOST_PORT/ADMIN_*/NAV_TIMEOUT, DEFAULT_TAG_MAP/DB_IMAGE/PS_DOMAIN/STARTUP_TIMEOUT, DEFAULT_RULES. Grep untuk reports/modules dir -> KOSONG. Jadi psm_reports_dir & psm_modules_dir tak punya default kanonik sisi-skrip apa pun. psm_reports_dir justru key terpakai terberat di SKILL.md (6 sebutan: konvensi file lapis, --screenshot-dir, -o agregat, path memlog) dan ps-plan-layers.py:191 menandainya required=True TANPA default — jadi di jalur resolver-absen model WAJIB memasok nilai yang janji fallback-nya tak sediakan. Satu-satunya bentuk tertulis yang pernah dilihat model adalah bentuk BER-TOKEN ({project-root}/_bmad-output/psm-validate di config.yaml & module.yaml), dan tak ada konsumen yang menolak token (customization-3).
- Recommendation: Jangan bayar dgn token SKILL.md. Tutup di skrip: beri --reports-dir default kanonik DEFAULT_REPORTS_DIR='_bmad-output/psm-validate' relatif cwd (bentuk TANPA token, sehingga janji fallback jadi benar & tak memasok bahan token), lalu masukkan ke gerbang drift terhadap PSM_DEFAULTS (bandingkan setelah menanggalkan prefiks {project-root}/). Alternatif lebih murah: koreksi SKILL.md:19 agar fallback menyebut hanya key yang benar-benar punya default skrip — tapi itu menambah prosa.

#### customization-3 — reject_unresolved_paths: kelas BELUM tertutup di konsumen — 3x di psm-setup, 0x di psm-validate yang konsumen terberat

- Lens: customization
- Location: `scripts/ps-plan-layers.py (menelan token, exit 0); bandingkan psm-setup/scripts/{cleanup-legacy.py:261, merge-config.py:499, merge-help-csv.py:142}`
- Evidence: grep 'project-root|unresolved' di scripts/ psm-validate -> KOSONG (nol penjaga), sementara psm-setup memikul reject_unresolved_paths TIGA kali. Token BISA sampai titik pakai & dilaporkan sbg vonis percaya-diri: ps-plan-layers --reports-dir '{project-root}/_bmad-output/psm-validate' -> EXIT=0, semua lapis rerun=True, reason 'file lapis belum ada', path '{project-root}/...' HARFIAH. Persis kegagalan yang docstring expand_project_root namai: 'ps-plan-layers exit 0 sambil melihat folder harfiah, melaporkan file lapis belum ada dgn percaya diri, lalu setiap lapis mahal dijalankan ulang'. Fix ronde-4 menutup TITIK EMIT saja; docstring-nya mengklaim 'mengekspansi di sini menghapus KELASNYA' — klaim itu PATAH: konsumennya tetap telanjang, dan customization-2 menunjukkan jalur nyata (resolver absen) yang melewati emit sepenuhnya. Seam sebentuk lain tak seragam: ps-static-scan -o ber-token -> EXIT=1 + traceback mentah (sementara SKILL.md:37 mengajarkan 'Exit 2 = error input'); ps-e2e-run:836 mkdir atas --screenshot-dir ber-token akan MEMBUAT pohon sampah berfolder harfiah {project-root}.
- Recommendation: Pindahkan reject_unresolved_paths jadi milik-bersama (satu pemilik, mis. di ps-static-scan yang sudah jadi sibling import) dan panggil di SETIAP seam penerima path config: --reports-dir, -o/--output (kelima skrip), --screenshot-dir, --rules/--extra-rules. Tolak exit 2 + pesan menyebut key config-nya, selaras kontrak SKILL.md:37. Ini menutup KELAS di sisi konsumen: ekspansi resolver jadi sabuk pertama, penolakan konsumen sabuk kedua, dan jalur resolver-absen tak lagi bisa menyelundupkan token.

#### customization-4 — Salinan KEEMPAT tag map hidup di prosa quickstart tanpa penjaga — padahal gerbang doc-vs-konstanta sudah ada untuk string lain

- Lens: customization
- Location: `references/e2e-quickstart.md:25,116 vs tests/test-ps-flashlight-run.py:256-258 (gerbang doc yang SUDAH ada)`
- Evidence: Nilai tag map punya 4 salinan: PSM_DEFAULTS, DEFAULT_TAG_MAP (:48), config.yaml + module.yaml, dan PROSA di quickstart:25 ('for t in 1.7.8.11 8.1.6-nginx 9.1.4-nginx; do') + :116. Tiga yang pertama dijaga; yang keempat tidak. MUTASI (naikkan tag 9.1 secara KONSISTEN di resolver + skrip + config.yaml + module.yaml, doc dibiarkan): suite psm-validate 'SEMUA TEST LOLOS' sementara doc masih menyebut tag lama. Doc stale lolos penuh. KELAS YANG SUDAH PERNAH TER-SHIP: komentar test-nya sendiri mencatat 'drift ini pernah ter-ship (tag 9.0=nightly & 8.1=8.1 usang) dan ditemukan user, bukan CI'. Mekanismenya SUDAH ADA — test:256-258 sudah menegakkan konstanta-vs-doc untuk perintah cleanup di SKILL.md & quickstart — hanya tak diterapkan ke nilai tag. Baris 25 khusus berbahaya: ia perintah docker pull yang dijalankan operator untuk menyiapkan mesin; tag stale = image yang ditarik BUKAN image yang diuji Lapis 2/4.
- Recommendation: Perluas gerbang doc yang sudah ada (pola persis sama, nol token SKILL.md): untuk tiap tag di DEFAULT_TAG_MAP.values(), assert tag muncul di references/e2e-quickstart.md, dan assert tak ada tag di luar himpunan itu berpola <versi>-nginx/1.7.* tercecer di doc. Lebih baik: buat baris 25 menurunkan tag-nya dari skrip sehingga salinan keempat lenyap alih-alih dijaga.

#### enhancement-4 — REMOVE: e2e_scenario_sources di agregat — echo tanpa pembaca yang menawarkan ulang premis struktural yang sudah didiskreditkan

- Lens: enhancement
- Location: `scripts/ps-aggregate.py:750-755`
- Evidence: Vonis atas item inventaris (usul DELETE) — dikonfirmasi, dgn alasan lebih tajam dari sekadar 'yatim'. Grep pemanggil: ps-e2e-run:959 memproduksi scenario_sources (di file Lapis 4); ps-aggregate:753-755 meng-echo jadi e2e_scenario_sources; SKILL.md -> 0 rujukan; quickstart:184 menyuruh manusia cek scenario_sources yaitu field di FILE LAPIS 4, bukan echo ini. Jadi echo agregat tak punya satu pun pembaca: bukan gerbang, bukan rute prompt, bukan dokumen. Komentar pembenarnya (:750-752) sudah kedaluwarsa: pekerjaan itu kini dipikul e2e_smoke_only (dihitung dari authored_assertions, MENJATUHKAN ready, DIRUTEKAN SKILL.md:39). Lebih buruk dari netral: daftar nama file adalah persis premis STRUKTURAL yang docstring count_authored_assertions hukum mati ('sebuah spec berisi dua screenshot — nol assertion — menjawabnya ya juga'). Membiarkannya di top-level mengundang pembaca berikutnya menarik kesimpulan yang sudah dibayar mahal untuk dibuang.
- Recommendation: Hapus result['e2e_scenario_sources'] beserta variabel e2e_sources (:753-755), sisakan e2e_smoke_only yang tetap butuh blok if e2e_ran, rapikan komentar :750-752. Yang hilang: tak ada — provenance skenario tetap utuh di scenario_sources file Lapis 4, tempat quickstart:184 sudah mengarahkan pembaca. Nol perubahan SKILL.md.

### Low (4)

#### leanness-4 — Overview menarasikan ulang keempat lapis — salinan ke-3 setelah frontmatter dan header Lapis

- Lens: leanness
- Location: `SKILL.md:10 (Overview)`
- Evidence: Overview:10 memuat 'empat lapis: aturan deterministik yang diketahui pasti (skrip), perilaku nyata terhadap PrestaShop core asli di Docker flashlight, review adversarial e-commerce (judgment), dan uji perilaku browser (E2E) terhadap core yang benar-benar boot' — 69 token. Frontmatter description sudah menyatakannya, dan Validate:27-33 mendefinisikan keempatnya sbg header bold beserta pemicu & flag-nya. Tiga salinan dari daftar yang sama; kanon menyebutnya 'facts restated across sections'. GREP WIRE: yang BUKAN restatement adalah pembagian (skrip) vs (judgment) — pembelahan determinisme yang mengatur seluruh skill, dan Overview satu-satunya tempat SKILL.md menyatakannya sbg pembagian. Maka ini TRUNCATE, bukan DELETE.
- Recommendation: Truncate: pertahankan pembelahan skrip/judgment yang non-inferable, buang re-deskripsi tiap lapis yang sudah dimiliki header Validate.
- Proposed smallest: '...empat lapis, tiga dimiliki skrip (pindai statis, flashlight, E2E) dan satu judgment-mu (adversarial).'
- Predicted delta: Diprediksi kehilangan kecil-tapi-nyata, sengaja tak diklaim nol: frasa 'core asli'/'core yang benar-benar boot' adalah satu-satunya tempat SKILL.md menyebut bahwa flashlight = PrestaShop core sungguhan, dan itu yang membenarkan biaya Lapis 2/4. Terukur: 69 -> 34 token (-35). Ambil HANYA bila headroom dibutuhkan setelah leanness-1..3 mendarat (paket lengkap 2995 -> 2936); bila tidak, ini yang paling lemah dari keempatnya dan boleh dilewati.

#### architecture-5 — 6.6 MB output analisis builder ter-track di dalam folder skill

- Lens: architecture
- Location: `.analysis/ (82 file ter-track)`
- Evidence: du -sh .analysis/ -> 6.6M; git ls-files | wc -l -> 82; git check-ignore -> (not ignored). Isinya 14 ronde laporan yang sudah di-supersede, termasuk artefak dari bug scanner builder yang kini sudah diperbaiki (lint-paths.json membengkak 1.9 KB -> 2.8 MB lintas ronde, lalu kembali 828 B di ronde terakhir — bug-nya hilang, bangkainya tetap ter-commit). Tak dimuat di jalur run manapun dan tak memakan token, jadi dampaknya distribusi bukan eksekusi — tapi references/e2e-quickstart.md:193 secara eksplisit menyuruh menyalin skill ini ke mesin lain, dan yang tersalin 6.6 MB laporan mati. Dicatat terpisah dari keputusan user #6: .memlog.md (95 KB) adalah memori builder yang AKTIF dipakai resume; .analysis/ adalah riwayat yang sudah dipegang git.
- Recommendation: Tambah .analysis/ ke .gitignore dan hapus dari index (git rm -r --cached), sisakan ronde terakhir bila builder membutuhkannya sbg baseline. Riwayatnya tetap ada di git history. Jangan sentuh .memlog.md.

#### customization-5 — --extra-tag-map dari knowledge base MENGGANTI tag config untuk versi yang bertabrakan, sementara SKILL.md menyebutnya 'MENAMBAH'

- Lens: customization
- Location: `SKILL.md:21 vs scripts/ps-flashlight-run.py:168-178 (parse_tag_map)`
- Evidence: SKILL.md:21 menyatakan '--extra-rules/--extra-tag-map MENAMBAH, sedangkan --rules/--tag-map MENGGANTI'. Perilaku nyata: parse_tag_map(base, '9.1=nightly') -> config saja {9.1:'9.1.4-nginx'}; config+extra -> {9.1:'nightly'}. Untuk versi yang SUDAH ada di peta config, base.update(_parse_pairs(extra)) membuat extra MENANG — ia mengganti, bukan menambah. Akibatnya _bmad/psm/memory/tech/flashlight.md (file memori yang bisa berbulan-bulan lebih tua) diam-diam mengalahkan psm_flashlight_tag_map yang baru saja diset user — surface kanonik kalah oleh surface kedua, dan model yang membaca 'MENAMBAH' akan menyangka tag config selamat. Presedensinya benar & disengaja di level skrip (docstring:170-173: 'menambal satu-dua versi tanpa menjatuhkan sisanya'), jadi cacatnya ada pada PROSA di tempat model membacanya.
- Recommendation: Headroom cuma 5 token — jangan tambah kalimat, ubah kata saja: 'keduanya MENAMBAH' -> 'keduanya MENAMBAL (versi/aturan yang sama: yang tambahan menang)'. Nol token bersih, presedensinya jadi tertulis di titik baca. Pertimbangkan ps-flashlight-run mencatat tabrakan ke output (tag_map_overridden) — tapi HANYA bila ada konsumen yang membacanya, jangan lahirkan sinyal yatim baru.

#### enhancement-5 — REMOVE: plan.e2e_scenarios (echo yatim); sisa inventaris echo + 'nama bentrok' dinilai DERAU — jangan dibuka lagi

- Lens: enhancement
- Location: `scripts/ps-plan-layers.py:167-185, :247-250`
- Evidence: Sisa inventaris direproduksi satu per satu. (a) LAYAK, kecil: plan.e2e_scenarios -> ['configure.json'], hanya daftar nama file (18 char, 1% output), duplikat scenario_sources Lapis 4, nol pembaca (SKILL.md:25 hanya menyuruh jalankan yang rerun); ia menyeret lazy by-path import _e2e_scenarios (~20 baris). (b) 'NAMA BENTROK e2e_scenario_notes' — BUKAN cacat, TOLAK: plan & agregat mengemit isi SAMA dari sumber SAMA (discover_scenarios, satu pemilik); nama sama untuk makna sama = 'One term per concept', justru benar. Klaim 'pra-pass memperingatkan ke ruang hampa' juga tak bertahan: outputnya JSON 981 char yang model memang dibaca untuk mencari rerun — catatan itu duduk di sebelahnya. (c) Echo bernilai rendah (module_path/major/kind/version/db_image/orchestrator/headed/findings[].browser|scenario|source|versions, occurrences[].snippet, e2e versions[].image/install.no_console/no_psroot) — DERAU, TOLAK: ini output JSON diagnostik yang manusia debug; provenance run adalah nilainya, dan no_console/no_psroot memang dibaca internal di ps-e2e-run:812 untuk membentuk pesan error jujur. (d) Per-versi pass tandingan — di luar lensa ini (topologi vonis).
- Recommendation: Hapus result['e2e_scenarios'] (:248) dan ubah _e2e_scenarios agar mengembalikan notes saja; pertahankan e2e_scenario_notes. Yang hilang: tak ada — nama spec sudah ada di scenario_sources Lapis 4 dan di folder module. Untuk (b)(c)(d): catat sbg DITOLAK dgn bukti di atas, jangan dijadwalkan.
