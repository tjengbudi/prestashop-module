# Analysis Report: /home/budi/dev/prestashop-module/.claude/skills/psm-validate

Generated: 2026-07-17 · Schema: 2

**Grade: Poor**

> Round 3's fixes are real — every one survived mutation, the siblings read `ready`, and for the first time in four rounds there is no dead wire — but the defect class outlived them again: the adversarial reviewer's own scope declaration is enforced by ps-plan-layers and ignored by ps-aggregate, so `ready` goes true over versions the reviewer said it never reviewed.

This skill's scripts genuinely own the deterministic work and its honest-degrade vocabulary is unusually well developed — the lenses verified round-3's fifteen closures by reproduction and mutation rather than trusting the claims, and all of them held. The primary opportunity is that the release gate does not consume the honesty signals the pipeline already computes: the adversarial layer's declared review scope, `e2e_smoke_only`, and static's version coverage are each calculated correctly and then dropped before `ready` is decided, which is how a module gets a confident four-layer green over work nobody did. Underneath sits the pattern four rounds have not shaken — a fix lands precisely on the site the finding named while the same rule, implemented a second time one seam over, keeps the old behavior.

| Severity | Count |
| --- | --- |
| Critical | 1 |
| High | 5 |
| Medium | 9 |
| Low | 4 |

## Themes

### 1. Coverage is measured, then dropped before the gate reads it

- Root cause: Three separate signals answer "did we actually test this?" — the adversarial payload's top-level `versions`, `e2e_smoke_only`, and the set of versions static-scan really produced. All three are computed correctly. None reaches compute_ready or merge_version, so `ready`/`pass` claim exactly the coverage compute_ready's own docstring promises they never will. The perverse consequence is measurable: deleting a broken tests/e2e spec flips ready from false to true, so the gate rewards having no tests.
- Fix: Route every coverage signal into the one channel compute_ready already honours (the layer's conclusive flag + inconclusive_note). Adversarial: reuse ps-plan-layers.layer_versions inside adversarial_layer so a target version outside the reviewer's declared scope returns conclusive=False, and make top-level `versions` a required, validated field in validate_adversarial. E2E: when e2e_smoke_only is true, set the layer's inconclusive_note so ready falls. Static: merge_version must honour static_layer's "versi tak ada di hasil static-scan" reason instead of reading absence of findings as pass. Each needs a test that goes red when the wiring is cut.
- Findings:
  - `determinism-1` Adversarial layer's declared review scope is enforced by ps-plan-layers and ignored by ps-aggregate — ready=true over versions the reviewer said it never reviewed — `scripts/ps-aggregate.py:262-277 (adversarial_layer), :219-259 (validate_adversarial); references/adversarial-lens.md:50-52`
  - `determinism-4` compute_ready never reads e2e_smoke_only — ready=true when Layer 4 proved nothing about the module, and authoring a spec that breaks makes ready WORSE than authoring none — `scripts/ps-aggregate.py:31-52 (compute_ready), :388-394 (e2e_smoke_only); SKILL.md:39`
  - `determinism-6` ps-aggregate reports pass=true and exit 0 for target versions that static-scan never scanned — `scripts/ps-aggregate.py:63-76 (static_layer), :280-298 (merge_version), :323-326`

### 2. One rule, several implementations — and the seam that has none

- Root cause: The recurring shape, now visible across the whole family: a question with one correct answer gets answered by whatever string-matching is local to each call site. is_skipped is the sanctioned owner of "is this file module source" and has exactly one call site, while two other paths in the same file hand-roll it — one of them reproducing the very absolute-path bug is_skipped's docstring says it fixed. find_main_file's filename heuristic answers "is this a PrestaShop module" and now gates a terminal stop. The canonical defaults live in the resolver and are restated in six script literals that SKILL.md blesses as a fallback source of truth. And reject_unresolved_paths exists three times in psm-setup while psm-validate — the heaviest consumer of the path — has no copy.
- Fix: For each, give the question one owner and make the other seams call it: route the iter_files glob branch and scan_structure_rule's `"vendor" not in p.parts` through is_skipped; split find_main_file's conflated answers into distinct reason codes so the stop fires only on "no PHP at root" and the ambiguous case reaches the model; port the family's reject_unresolved_paths into the psm-validate scripts that receive --reports-dir/-o/--screenshot-dir (or better, expand {project-root} in the resolver on emit); and extend the existing anti-drift test to import the script defaults and assert them against PSM_DEFAULTS.
- Findings:
  - `determinism-2` is_skipped has one call site; two other paths in the same file hand-roll the skip — third-party vendor code produces blocking errors, and a module under an ancestor named vendor/ is silently unscanned — `scripts/ps-static-scan.py:66-77 (iter_files glob branch), :118 (scan_structure_rule index_php_each_dir)`
  - `determinism-5` find_main_file's filename heuristic decides "is this a PrestaShop module" and now gates a terminal stop that mislabels a real module — `scripts/ps-static-scan.py:57-64 (find_main_file); SKILL.md:27, :45`
  - `customization-1` Resolved psm_reports_dir reaches its point of use with {project-root} unexpanded, and no psm-validate script rejects it — `psm-setup/scripts/resolve-psm-config.py:53-54, 102-119; SKILL.md:19,25,33,37,43; scripts/ps-plan-layers.py:106; scripts/ps-e2e-run.py:743`
  - `customization-2` Layer scripts hold a second, unguarded copy of the canonical defaults that SKILL.md sanctions as a fallback source of truth — `scripts/ps-flashlight-run.py:48-52, :447; scripts/ps-e2e-run.py:82, :800; scripts/ps-static-scan.py:258; scripts/ps-plan-layers.py:107; vs psm-setup/scripts/resolve-psm-config.py:39-55`

### 3. The cheap pre-flight is blind to half of what it gates

- Root cause: ps-plan-layers exists so expensive layers are not re-run on fresh evidence, and SKILL.md forbids the model from second-guessing it. But it defines freshness as module-source mtime alone, while the static verdict has two inputs — the module AND the ruleset. Change a knowledge-base rule and the stale layer file is reused, so the new blocking rule never fires. The same pre-flight never opens the authored E2E specs, so a typo in the one input a human hand-writes costs a full multi-version Docker run to discover, and the note that reports it does not name the valid vocabulary sitting in SUPPORTED_ACTIONS.
- Fix: Widen the pre-flight to the whole question: have ps-static-scan record the ruleset provenance (paths+mtimes, or a hash of merged rule ids) in its output and let plan_layer compare against the current ruleset rather than re-deriving from source mtime alone. Have ps-plan-layers validate <module>/tests/e2e/*.json by importing the check from ps-e2e-run, surfacing the notes before any container boots, and append the valid action list to the unknown-action note. Keep both non-blocking — the universal smoke still has value with a broken spec.
- Findings:
  - `determinism-3` ps-plan-layers defines freshness as module-source mtime only — a changed ruleset reuses a stale static layer, and SKILL.md forbids the model from backstopping it — `scripts/ps-plan-layers.py:41-54 (newest_source_mtime), :76-98 (plan_layer); SKILL.md:25`
  - `enhancement-1` Add: Plan-validate-execute is missing its load-bearing validation piece for authored E2E specs — `scripts/ps-e2e-run.py:246-274 (discover_scenarios), scripts/ps-plan-layers.py, SKILL.md:25`

### 4. Run-path knowledge parked in a file the run path never opens

- Root cause: e2e-quickstart.md is correctly routed for setup and the TDD loop, but two of its gotchas fire during a normal validating run, when nothing loads it. Screenshots are written with deterministic names, no timestamp and no cleanup, so a scope-narrowed run reviews last week's PNGs from a version no longer in scope — and SKILL.md's vision step turns a believed visual defect into a blocking adversarial error. Adjacent: the orchestrator is told to write the schema-gated adversarial file without being routed to its schema or told to append rather than replace, and the inline `playwright install` command contradicts the reference's own version-match warning one parenthesis away.
- Fix: Pull the run-path gotchas inline as clauses on the lines that trigger them (review only the paths the run's JSON lists; port-leak recovery on the degrade clause), or remove the screenshot gotcha at its root by writing shots to a per-run subfolder. State the merge rule where the second write happens — append to `findings`, preserve top-level `versions`, shape is in references/adversarial-lens.md. Fix the install command to `uv run --with playwright playwright install <engine>` or drop it and keep the routing. Add the quickstart pointer to the write-a-scenario offer.
- Findings:
  - `architecture-1` Layer-4 run-path gotchas are carved into e2e-quickstart.md, which the run path never loads — the vision-review branch can block on a stale screenshot — `SKILL.md:33 (Lapis 4, verifikasi visual) + references/e2e-quickstart.md:143-162 (## 4. Gotchas); scripts/ps-e2e-run.py:306-319, :739-748`
  - `architecture-2` The orchestrator is told to write the adversarial layer file but is never routed to its schema, and no merge rule governs the second writer — `SKILL.md:31 (Lapis 3), :33 (Lapis 4 visual defect), :37 (exit 2) + references/adversarial-lens.md:28-52`
  - `architecture-4` The inline `playwright install` prerequisite contradicts the reference's own version-match warning one parenthesis away — `SKILL.md:33 vs references/e2e-quickstart.md:20-22, :30-32; scripts/ps-e2e-run.py:1-5 (PEP723 header)`
  - `enhancement-2` Add: the offer-the-fix for smoke-only E2E doesn't name where the fix is documented — `SKILL.md:39`

### 5. Prose re-teaches what the scripts own — and there is no budget left to pay for the fixes

- Root cause: The leanness rebound, fourth round running. SKILL.md sits at 2987 of 3000 tokens, and roughly 350 of them narrate mechanics the scripts execute and self-label: the flashlight DB pairing and neon rules that --help already carries at point of use, the skipped/skipped_image/not-conclusive taxonomy the output delivers in plain language, a paraphrase of the E2E --help immediately before pointing at --help, and a near-verbatim restatement of --require's help text defending a design choice the reader cannot revisit. The Overview closes by promising three rule outcomes the ruleset enforces natively and the model is forbidden to act on. Every fix above needs tokens this recovers.
- Fix: Take the four proposed_smallest rewrites (they carry predicted deltas and are worth routing to a variant eval before cutting), keep the invocations, the config-to-flag mappings, the image-pull consent and the visual-verification wiring untouched, and lift the Lapis 4 visual mechanics into references/e2e-quickstart.md while the trigger stays inline. That is ~350 tokens back — do this first, so the determinism and architecture fixes have room to land without another over-budget scramble.
- Findings:
  - `leanness-1` Overview closes with a rule the ruleset enforces natively and the model is forbidden to act on — `SKILL.md:10 (Overview, final sentence)`
  - `leanness-2` Lapis 2 narrates flashlight internals and a degrade taxonomy the script executes and self-labels — `SKILL.md:29 (Lapis 2 paragraph, 218 tok)`
  - `leanness-3` Lapis 4 restates what ps-e2e-run.py does immediately before telling the model not to re-judge it — `SKILL.md:33 (Lapis 4 paragraph, 347 tok — the largest in the file)`
  - `leanness-4` Headless section re-argues the `ready` design decision that --require's help text already makes verbatim — `SKILL.md:45 (Mode headless, second paragraph — 129 tok of a 250 tok paragraph)`
  - `architecture-3` SKILL.md sits at 2987 of a 3000-token budget — in the warn tier with 13 tokens of headroom — `SKILL.md (whole file); budget from bmad-workflow-builder/customize.toml:58-59`

## Strengths

- Round 3's fifteen closures are real, and were proven so rather than assumed: the determinism lens mutated each of the seven script-side fixes and every one went red, the enhancement lens confirmed all four sibling workflows now read `ready` with an explicit not-`pass` rationale, and the customization lens found no dead wire for the first time in four rounds — all twelve resolved config keys reach a consumer.
- The four false-pass invariants hold by construction, including the round-3 CRITICAL: fatal detection now takes the raw response body, expect_text filters on visibility, and findings/errors remain mutually exclusive.
- The intelligence-placement boundary is right in the large: five scripts own the deterministic work with 253 asserts across five suites, lint is clean (scan-scripts 0/0/0, path 0 on the live tree, integrity 0), and the model's one sanctioned channel into the verdict — the vision pass writing into the adversarial file before aggregate runs — is genuinely non-inferable wiring that earns its prose.
- The honest-degrade vocabulary (skipped / skipped_image / skipped_browser / not-conclusive / inconclusive_note / e2e_smoke_only / layers_run / required_layers) is unusually well developed, and the strict-by-default --require was chosen over the flattering default when a verifier refuted the first attempt.
- Declining customize.toml was correct and is holding: the resolver really is the only config mechanism, so no second surface competes with it.
- The working rules recorded in the memlog after three rounds of refuted closures — reproduce don't read, every fix needs a test proven able to fail, fix the class not the site, the fixer is not a good judge — are what made this round's verification trustworthy, and they caught real vacuous tests.

## Recommendations

1. Close the coverage gate: make the adversarial scope, e2e_smoke_only, and static's version coverage each reach compute_ready/merge_version through the conclusive+note channel that already exists. This is the CRITICAL and it is the skill's core promise — three signals already computed, none consumed. (resolves: determinism-1, determinism-4, determinism-6)
2. Trim the ~350 tokens of script-owned narration first (four proposed_smallest rewrites plus lifting the Lapis 4 visual mechanics into the reference), so every other fix has budget to land instead of forcing another over-budget scramble. (resolves: leanness-1, leanness-2, leanness-3, leanness-4, architecture-3)
3. Give each duplicated rule one owner and make the other seams call it — is_skipped for every file-is-module-source decision, distinct reason codes behind find_main_file's stop, the family's reject_unresolved_paths (or resolver-side expansion) for {project-root} paths, and an anti-drift test that imports the script defaults. (resolves: determinism-2, determinism-5, customization-1, customization-2)
4. Bring the run-path gotchas to the lines that trigger them (screenshot provenance, port-leak recovery, the adversarial append rule, the correct playwright install command, the quickstart pointer on the write-a-scenario offer). (resolves: architecture-1, architecture-2, architecture-4, enhancement-2)
5. Widen the pre-flight to the whole question: ruleset provenance in the freshness decision, and authored E2E spec validation before any container boots, with the note naming the valid action vocabulary. (resolves: determinism-3, enhancement-1)

## Experience

- **Operator validating a module before release (interactive)** — Resolver loads config -> scope menu offers versions from psm_target_versions and browsers from psm_e2e_browsers (a recorded user decision, working as designed) -> ps-plan-layers marks which layers need a rerun -> Layer 1 static (stops honestly on a non-module folder) -> Layer 2 flashlight with image-pull consent -> Layer 3 delegated adversarial reviewer -> Layer 4 browser E2E with a vision pass over screenshots -> ps-aggregate merges into ready/pass. The path works end-to-end and has been proven against real containers on all three versions; the live risk is that the verdict at the end can read greener than the evidence supports (rank 1).
- **Sibling workflow using psm-validate as a quality gate (headless)** — psm-develop / psm-scaffold / psm-cross-version / psm-optimize invoke it headless, and all four were verified to read `ready` rather than `pass` — the round-3 closure held. They inherit rank-1's exposure: a module can hand them ready=true over versions the reviewer never reviewed or behavior E2E never exercised.
- **Author writing an E2E scenario (TDD loop)** — Writes tests/e2e/*.json against the action table in references/e2e-quickstart.md, drives red -> green across Chromium and Firefox. The reference is strong (verified cross-version BO pattern, real gotchas), but a spec typo is only discovered after a full multi-version Docker run, and the note that reports it does not name the valid actions (rank 5).
- Headless: Genuinely well built and the strongest of the three modes: no clarifying questions, never pulls an image or downloads a browser without a human, returns a small JSON object with status/ready/pass/conclusivity rather than prose, has a real `blocked` channel with reasons, and records its assumptions via memlog init-then-append (a bug a verifier caught by reproducing the first-run FileNotFoundError). The strict-by-default --require means a runner without Docker honestly reports ready:false instead of a flattering green.

## Findings

### Critical (1)

#### determinism-1 — Adversarial layer's declared review scope is enforced by ps-plan-layers and ignored by ps-aggregate — ready=true over versions the reviewer said it never reviewed

- Lens: determinism
- Location: `scripts/ps-aggregate.py:262-277 (adversarial_layer), :219-259 (validate_adversarial); references/adversarial-lens.md:50-52`
- Evidence: RAN: aggregate with static.json (1.7.8,8.1,9.1 clean) + adv.json = {"versions": ["8.1"], "findings": []} — the reviewer honestly declaring per adversarial-lens.md:31 that it reviewed ONLY 8.1. Produced: ready: True, and adversarial ran=True conclusive=True on all three versions including 1.7.8 and 9.1. RAN ps-plan-layers.py on the identical file: reuse: False | cakupan kurang: 1.7.8, 9.1 — the sibling script reads the same field and correctly refuses. grep confirms ps-aggregate.py contains no scope/coverage check, and validate_adversarial() validates findings element-wise but never the top-level versions the contract requires. No aggregate test covers it. compute_ready's own docstring states the invariant this breaks: "kalau tidak, ready mengklaim persis coverage yang tak diuji". Adversarial is in the default --require set and SKILL.md:45 says CI reads ready. This is the one layer whose payload the model (not a script) produces, and the only one whose coverage claim goes unchecked.
- Recommendation: Determinism test: "does the adversarial file's declared scope cover this version?" has one correct answer per input — it belongs in the script, and the script that already computes it (ps-plan-layers.layer_versions) proves it. Reuse that function in ps-aggregate: adversarial_layer(...) must take the payload's top-level versions and return conclusive=False + reason='versi tak ditinjau reviewer' for any target version outside it. Add the field to validate_adversarial as required (exit 2 when absent or not a list of target-resolving strings, using the existing _version_matches), so the model->script seam gate covers the whole contract rather than just findings.

### High (5)

#### determinism-2 — is_skipped has one call site; two other paths in the same file hand-roll the skip — third-party vendor code produces blocking errors, and a module under an ancestor named vendor/ is silently unscanned

- Lens: determinism
- Location: `scripts/ps-static-scan.py:66-77 (iter_files glob branch), :118 (scan_structure_rule index_php_each_dir)`
- Evidence: grep: is_skipped is called at exactly ONE site (:76, the suffix branch). :118 hand-rolls `"vendor" not in p.parts` (absolute parts, and blind to node_modules/.git); :67-73 (the files: glob branch) applies no skip at all. RAN static-scan on a clean fixture module: struct-index-php fired on node_modules and .git, and smarty-unescaped fired on .git/hook.tpl, vendor/acme/src/vend.tpl, node_modules/pkg/nm.tpl — all third-party files. RAN with a schema-valid --extra-rules exactly as SKILL.md:21's knowledge-base path supplies it ({severity: error, files: ["*.php"], pattern: eval}): module source was clean, yet exit code 1, pass: False, BLOCKING kb-no-eval at vendor/acme/src/bad.php — a clean module failed on third-party code. RAN the same module copied under an ancestor dir literally named vendor/: struct-index-php fired 2x outside it and 0x under it (findings: []) — the exact absolute-path failure is_skipped's docstring says it fixed. The fix landed on is_skipped and its ps-plan-layers import; these two sites kept the old technique.
- Recommendation: Intelligence leak: three different string-matching techniques each deciding "is this file module source", a meaning question with one owner. Route every path through is_skipped(p, module_dir): filter the iter_files glob branch and replace :118's `"vendor" not in p.parts` with `not is_skipped(d, module_dir)`. Guard it with a test that asserts zero findings under vendor/, node_modules/ and .git/ via BOTH the glob branch and the structural branch, and one that scans a module nested under an ancestor named vendor/ — mutating is_skipped back to a substring match must turn those red (the existing is_skipped tests do go red, so the technique is proven; only these call sites are unreached).

#### determinism-3 — ps-plan-layers defines freshness as module-source mtime only — a changed ruleset reuses a stale static layer, and SKILL.md forbids the model from backstopping it

- Lens: determinism
- Location: `scripts/ps-plan-layers.py:41-54 (newest_source_mtime), :76-98 (plan_layer); SKILL.md:25`
- Evidence: RAN: wrote reports/mymod-static.json for a module using hook actionKbNewRule, then (1s later) created an extra-rules.json declaring that hook removed in PS9 — the knowledge-base augmentation SKILL.md:21 explicitly supports. ps-plan-layers then reported static reuse: True | lebih baru dari module & cakupan versi cocok, rerun list omits static. But RAN static-scan --extra-rules on the same module: pass: False | errors: 1 | ['kb-hook-new']. So the reused layer file reports pass=True and the new blocking rule never fires. The verdict has two inputs — module source AND ruleset (assets/ps-rules.json + --extra-rules) — and the freshness authority stats only the first. SKILL.md:25 closes the loop: "jangan menilai kesegaran file dengan tangan", so no model judgment catches it. Same exposure when the skill's own assets/ps-rules.json is updated (the version-defaults sync path).
- Recommendation: The determinism test passes on the full question, not half of it: "does this layer file still represent the module AND the rules that produced it?" Extend newest_source_mtime's comparison basis — give plan_layer a max(newest_source_mtime, ruleset mtimes) where ruleset = assets/ps-rules.json plus any --extra-rules path, and add --extra-rules/--rules flags to ps-plan-layers so it sees what Layer 1 will actually run with. Better: have ps-static-scan record the ruleset paths+mtimes (or a hash of the merged rule ids) in its output, and have plan_layer compare that recorded provenance against the current ruleset — then reuse is decided from what the file itself claims, not from a re-derivation. Test: touch the ruleset after writing the layer file, assert reuse=False.

#### determinism-4 — compute_ready never reads e2e_smoke_only — ready=true when Layer 4 proved nothing about the module, and authoring a spec that breaks makes ready WORSE than authoring none

- Lens: determinism
- Location: `scripts/ps-aggregate.py:31-52 (compute_ready), :388-394 (e2e_smoke_only); SKILL.md:39`
- Evidence: RAN aggregate twice with default --require (all four layers), static+flashlight+adversarial clean, Layer 4 conclusive and passing: (a) module with NO authored E2E specs -> ready: True | e2e_smoke_only: True; (b) identical, plus one authored spec that fails to parse (scenario_notes non-empty) -> ready: False. So deleting a broken tests/e2e/*.json flips ready from false to true, and a module whose behavior was never exercised gets the full four-layer green. grep: e2e_smoke_only is written at :394 and read by nobody — only asserted-as-reported by tests (test-ps-aggregate.py:389-397) and narrated by the model per SKILL.md:39. compute_ready's docstring names exactly this invariant — "lapis boleh konklusif ... sambil separuhnya tak pernah dievaluasi ... Itu tetap 'tak tuntas'" — and applies it to phpstan-absent via inconclusive_note but not to the smoke-only twin one seam over.
- Recommendation: Determinism leak: "did Layer 4 actually test this module, or only that the shop still renders?" has one correct answer already computed at :394, yet the release-readiness judgment is left to prose while the machine-read gate claims full proof. Feed it into the same channel the phpstan-absent case uses: when e2e_smoke_only is true, set the e2e layer's inconclusive_note ("hanya smoke universal — perilaku module tak teruji; tulis skenario di tests/e2e/") so compute_ready's existing note check makes ready=false. If four-layer ready is deliberately meant to be reachable without authored scenarios, that must be an explicit recorded choice like --require, not an omission — otherwise ready rewards having no tests. Test: assert ready is False for the smoke-only payload above, and that it goes red if the note is dropped.

#### architecture-1 — Layer-4 run-path gotchas are carved into e2e-quickstart.md, which the run path never loads — the vision-review branch can block on a stale screenshot

- Lens: architecture
- Location: `SKILL.md:33 (Lapis 4, verifikasi visual) + references/e2e-quickstart.md:143-162 (## 4. Gotchas); scripts/ps-e2e-run.py:306-319, :739-748`
- Evidence: SKILL.md:33 directs the model at a folder — --screenshot-dir <psm_reports_dir>/e2e-shots, then "tinjau screenshot kunci dengan penglihatanmu" — and SKILL.md:39 echoes e2e_screenshot_dir. Nothing in SKILL.md says to review only the paths this run emitted (per-version screenshots in the E2E JSON). ps-e2e-run.py names shots deterministically (<ver>/<engine>-<scenario>-<label>.png, :309/:741) with no cleanup and no timestamp (no rmtree/unlink/strftime in the file), so shots from prior runs survive under version folders no longer in scope. The binding scope menu makes exactly that reachable: narrow to 9.1, and last week's broken 8.1/ PNGs are still sitting in the folder. The caveat that would prevent it exists — e2e-quickstart.md:160-162 "screenshot bertanggal run ini (folder shots menumpuk file lama)" — but SKILL.md:33 routes that file only for "setup mesin baru + loop TDD E2E" and "Alat lain", neither of which fires on a normal validating run. The port-leak gotcha (e2e-quickstart.md:145-147) is stranded the same way: its trigger is a bind failure mid-run, a situation the model cannot route on. This is the principles' named failure — the model cannot load a file for a situation it does not know it is in — and here it inverts the skill's own honesty promise (SKILL.md:39 "Vonis tak pernah mengklaim coverage yang tak diuji"): a stale PNG becomes a confident error written into the adversarial layer file per SKILL.md:33, blocking a module on evidence from a run that no longer exists.
- Recommendation: Pull the two run-path gotchas inline as clauses on the lines that trigger them. On SKILL.md:33's visual step: review only the paths the run's JSON lists (per-version screenshots), never a folder listing — the folder accumulates prior runs. Add the port-leak recovery as a one-line gotcha on the Lapis 2/4 degrade clause. Leave setup, the TDD loop, the spec table and the BO interstitial pattern in e2e-quickstart.md — those have triggers the model can recognize and route on. Cheaper alternative for the screenshot half: have ps-e2e-run.py write shots under a per-run subfolder, which removes the gotcha instead of documenting it.

#### customization-1 — Resolved psm_reports_dir reaches its point of use with {project-root} unexpanded, and no psm-validate script rejects it

- Lens: customization
- Location: `psm-setup/scripts/resolve-psm-config.py:53-54, 102-119; SKILL.md:19,25,33,37,43; scripts/ps-plan-layers.py:106; scripts/ps-e2e-run.py:743`
- Evidence: Ran the resolver live with --project-root: it returns "psm_reports_dir": "{project-root}/_bmad-output/psm-validate" and "psm_modules_dir": "{project-root}/modules" — the token is NEVER expanded (resolve() at line 106 assigns the raw value; project_root is used only to locate _bmad/). SKILL.md:19 then instructs "baca apa adanya", and SKILL.md:25/33/37/43 pass <psm_reports_dir> straight into --reports-dir, --screenshot-dir and -o. Nothing on the receiving end guards it. Proven by running it: ps-plan-layers.py <mod> --reports-dir '{project-root}/_bmad-output/psm-validate' exits 0 and reports every layer as "reuse": false, "reason": "file lapis belum ada" with paths like {project-root}/_bmad-output/psm-validate/m-static.json. That is the misleading case, not merely a junk directory: SKILL.md:25 says don't judge freshness by hand, so the model trusts a confident pre-pass that has actually looked in the wrong place and re-runs every expensive layer. ps-e2e-run.py:743 mkdir(parents=True) would then materialize a literal {project-root}/ tree. The family already judged this failure real and guarded it three times over — merge-config.py:501, merge-help-csv.py:144, cleanup-legacy.py:263 each carry reject_unresolved_paths() whose docstring says failing loudly "prevents silently creating a junk {project-root}/ directory". psm-validate is the heaviest consumer of the value and is the one skill without the guard.
- Recommendation: Reuse the existing sibling guard rather than writing a fourth copy: port reject_unresolved_paths() into ps-plan-layers.py (--reports-dir), ps-aggregate.py (-o) and ps-e2e-run.py (--screenshot-dir) so a token-bearing path exits non-zero with the family's existing error instead of exit 0 with junk paths. Alternatively — and better, since it removes the failure class rather than catching it — have resolve-psm-config.py expand {project-root} against the --project-root it already resolves at line 143 before emitting the path-valued keys, which makes SKILL.md:19's "baca apa adanya" literally true. If expansion is chosen, keep config.yaml's stored form tokenized and expand only on emit.

### Medium (9)

#### determinism-5 — find_main_file's filename heuristic decides "is this a PrestaShop module" and now gates a terminal stop that mislabels a real module

- Lens: determinism
- Location: `scripts/ps-static-scan.py:57-64 (find_main_file); SKILL.md:27, :45`
- Evidence: RAN static-scan on mymod-master/ containing a valid mymod.php (correct class, ps_versions_compliancy), index.php, and one extra root php (autoload.php): main_file_found: False. SKILL.md:27 turns that into a hard stop — "berarti folder itu bukan module PrestaShop — berhenti" — and SKILL.md:45 makes it a blocked reason. The folder IS a PrestaShop module with a folder/file name mismatch; the script cannot distinguish that from "not a module", because its rule is <foldername>.php else "exactly one root .php". Last round's fix (gating the stop on main_file_found) made this misfire terminal rather than a compliancy error, so the fix widened the blast radius of the heuristic.
- Recommendation: Intelligence leak gating downstream behavior: a filename-count regex is answering a meaning question. Split the two answers the script conflates — emit main_file_found alongside a distinct reason code (no_php_at_root vs ambiguous_main_file vs folder_name_mismatch), and let SKILL.md stop only on the first. The ambiguous case is exactly where the model earns its place: it can read the candidates and tell which extends Module (or say the folder needs renaming to match, which is the actionable finding here — not "bukan module PrestaShop"). Cheap deterministic narrowing first: prefer the root .php whose contents match `extends\s+Module`, which resolves this fixture without judgment.

#### determinism-6 — ps-aggregate reports pass=true and exit 0 for target versions that static-scan never scanned

- Lens: determinism
- Location: `scripts/ps-aggregate.py:63-76 (static_layer), :280-298 (merge_version), :323-326`
- Evidence: RAN: ps-aggregate.py --static static.json --versions 1.7.8,8.1,9.1 where static.json covered only 8.1. Produced exit 0, overall pass: True, and for both 1.7.8 and 9.1: pass=True | static ran=False | versi tak ada di hasil static-scan. merge_version computes passed = (no blocking findings), and a version with no static entry contributes no findings, so absence of evidence reads as pass. ready is correctly False (static not conclusive), which contains the blast radius, but pass plus exit 0 is a silent-pass channel for a version never scanned — and SKILL.md:45 keeps pass as the field for reading "temuan yang benar-benar ditemukan".
- Recommendation: Comparison with one correct answer: target_versions vs the versions static actually produced. static_layer already detects it and returns a reason — merge_version should honour it rather than discard it. Set pass=False (or better, drop the version from the verdict and exit 2 as an input error, consistent with the schema-gate convention already used for the adversarial/extra-rules seams: a --versions that outruns the static input is a caller mistake, not a verdict). Test: aggregate --versions with a superset of static's keys must not return pass=True/exit 0.

#### architecture-2 — The orchestrator is told to write the adversarial layer file but is never routed to its schema, and no merge rule governs the second writer

- Lens: architecture
- Location: `SKILL.md:31 (Lapis 3), :33 (Lapis 4 visual defect), :37 (exit 2) + references/adversarial-lens.md:28-52`
- Evidence: Two writers touch one schema-gated file. Lapis 3 (SKILL.md:31) delegates to a subagent and passes references/adversarial-lens.md — the parent passes the path, correctly does not read it. Lapis 4 (SKILL.md:33) then has the orchestrator itself write a visual defect "ke file lapis adversarial sebagai error sebelum agregat jalan", with no statement of the file's shape and no instruction to append rather than replace. The shape is non-obvious and enforced: ps-aggregate.py:330-337 exits 2 on a severity token outside error|warning or a per-finding versions that does not resolve, and adversarial-lens.md:50-52 requires a top-level versions list that ps-plan-layers.py:57-73 (layer_versions) reads to decide reuse. An orchestrator that writes {"findings":[...]} from scratch either exits 2 (recoverable — SKILL.md:37 says fix and rerun) or silently drops the Layer-3 reviewer's findings and the top-level versions, and the aggregate cannot tell (ps-plan-layers.py:19-20 says so explicitly: it trusts the JSON handed to it). The lost-findings branch is a silent false pass, and it fires exactly when the model has found something.
- Recommendation: State the merge rule where the second write happens (SKILL.md:33): append to findings in the existing Lapis 3 file, preserving top-level versions — the shape is in references/adversarial-lens.md, read it before writing. That routing line also covers the Lapis 3 fallback branch (SKILL.md:31, skill running as a subagent, reviews itself), which today infers it from "kontrak yang sama".

#### customization-2 — Layer scripts hold a second, unguarded copy of the canonical defaults that SKILL.md sanctions as a fallback source of truth

- Lens: customization
- Location: `scripts/ps-flashlight-run.py:48-52, :447; scripts/ps-e2e-run.py:82, :800; scripts/ps-static-scan.py:258; scripts/ps-plan-layers.py:107; vs psm-setup/scripts/resolve-psm-config.py:39-55`
- Evidence: resolve-psm-config.py's docstring claims "Default di sini adalah satu-satunya sumber kebenaran untuk nilai yang absen". That claim is false. DEFAULT_TAG_MAP, DEFAULT_DB_IMAGE, DEFAULT_PS_DOMAIN, DEFAULT_STARTUP_TIMEOUT, DEFAULT_BROWSERS and four separate --versions default="1.7.8,8.1,9.1" literals restate the same canonical values, and SKILL.md:19 explicitly blesses them as an alternate path: "Resolver absen? Lanjut dengan default kanonik skrip" — so the scripts ARE a sanctioned second config source, not merely inert fallbacks. I verified the two copies are identical today, so this is a guard gap rather than a live divergence. But the guard genuinely does not exist: test-resolve-psm-config.py:159-179 (test_defaults_sync_module_yaml) cross-checks PSM_DEFAULTS against assets/module.yaml ONLY; lines 85-93 assert string literals, so an edit to either copy alone passes green. The comment at line 92, "e2e_browsers kanonik (selaras DEFAULT_BROWSERS skrip)", names the invariant it does not actually check. This exact drift already shipped once: .memlog.md:24 records config.yaml correct while the script fallbacks still carried the stale 9.0=nightly and 8.1=8.1 tags, found by the user rather than by a test.
- Recommendation: Extend the existing anti-drift test (test_defaults_sync_module_yaml already loads the resolver via importlib) with a second comparison that imports ps-flashlight-run.py and ps-e2e-run.py and asserts identity against PSM_DEFAULTS: parse_tag_map("") == PSM_DEFAULTS['psm_flashlight_tag_map'], DEFAULT_DB_IMAGE / DEFAULT_PS_DOMAIN / str(DEFAULT_STARTUP_TIMEOUT) / DEFAULT_BROWSERS against their keys, and each --versions default against psm_target_versions. That makes the three-copy invariant (module.yaml <-> PSM_DEFAULTS <-> script defaults) fail in CI instead of at a user's terminal. Either that, or drop the docstring's single-source-of-truth claim so it stops asserting a property nothing enforces.

#### leanness-1 — Overview closes with a rule the ruleset enforces natively and the model is forbidden to act on

- Lens: leanness
- Location: `SKILL.md:10 (Overview, final sentence)`
- Evidence: "Tidak pernah meloloskan module yang masih memakai dependency terlarang PS9, hook yang dihapus, atau tanpa ps_versions_compliancy" (37 tok) names exactly three rule families that assets/ps-rules.json already owns — forbidden_dependencies_ps9, removed_hooks, and rule struct-compliancy. Reproduced: a synthetic module with `use GuzzleHttp\Client` and no compliancy returned pass:false, ready:false with blocking finding dep-guzzle, with zero model involvement. Meanwhile SKILL.md:27 and :37 explicitly forbid the model from re-judging those findings or hand-assembling the verdict. The line therefore changes no move the reader can make — it is a promise about a decision the script has already taken. Canon core test: cannot name a failure its absence would produce.
- Recommendation: Cut the sentence. If the intent is to state the stance rather than the rule, it is already carried by "validator ... yang teliti dan jujur" plus "vonis berbasis bukti" earlier in the same paragraph.

#### leanness-2 — Lapis 2 narrates flashlight internals and a degrade taxonomy the script executes and self-labels

- Lens: leanness
- Location: `SKILL.md:29 (Lapis 2 paragraph, 218 tok)`
- Evidence: Two spans are meta-explanation of script-owned mechanics. (a) "Flashlight butuh DB terpisah (image-nya web-tier saja) — skrip mengurus pasangannya dan menjalankan phpstan ... (neon module ada = konklusif; auto-generate = advisory, tak memblok)" (60 tok) is a condensed restatement of ps-flashlight-run.py's --help epilog, which carries the identical DB-pairing and auto-neon/advisory explanation at point of use. The model's only move here is to invoke the script; it does not pair the DB or choose the neon. (b) "Docker/compose absen -> status: skipped; image belum ada lokal -> skipped_image; DB/boot gagal -> tak konklusif" (33 tok) pre-teaches a taxonomy the output already delivers in plain language: reproduced aggregate output contains {"state": "skipped", "conclusive": false, "reason": "Docker tidak tersedia — uji flashlight dilewati"}, and ps-aggregate consumes those states natively. The model never branches on them.
- Recommendation: Keep the invocation, the config->flag mapping, and the image-pull confirmation (a real consequence: a large download the user must consent to); drop the internals narration and the status taxonomy.
- Proposed smallest: **Lapis 2 — uji di flashlight per versi (bila Docker ada).** Jalankan `uv run scripts/ps-flashlight-run.py <module-path> --versions <target>` dan teruskan setelan config sebagai flag (`--tag-map` dari `psm_flashlight_tag_map`, `--db-image`, `--ps-domain`, `--orchestrator`, `--startup-timeout`) supaya sekali panggil langsung jalan (lihat `--help`). Skrip mengurus DB, phpstan, degrade, dan konklusivitas sendiri dan menandainya di output — jangan nilai ulang. Image flashlight besar: di sesi interaktif konfirmasi dulu, lalu ulangi dengan `--allow-image-pull`.
- Predicted delta: Nothing. The DB pairing and the neon conclusive/advisory rule are executed by the script regardless of what SKILL.md says, and the degrade states arrive in the output as `state` + a human-readable `reason` that the honest-reporting rule at SKILL.md:39 already tells the model to surface. Saves ~93 tok. Route to variant eval to confirm.

#### leanness-3 — Lapis 4 restates what ps-e2e-run.py does immediately before telling the model not to re-judge it

- Lens: leanness
- Location: `SKILL.md:33 (Lapis 4 paragraph, 347 tok — the largest in the file)`
- Evidence: "Skrip men-drive PrestaShop core asli lewat Playwright — smoke universal (shop tak rusak dengan module aktif) plus skenario authored module di <module-path>/tests/e2e/*.json — dan memvonis perilaku; lihat --help untuk format spec, jangan nilai ulang outputnya" (70 tok) is a paraphrase of the ps-e2e-run.py --help epilog, which states the same smoke-plus-authored-scenario description; the paragraph then points at --help anyway, so the reader who needs the detail fetches it and the reader who does not was told twice. The tail "Degrade jujur seperti Lapis 2 (plus skipped_browser bila browser absen)" (24 tok) repeats the taxonomy already flagged in leanness-2 — the script emits skipped_browser itself. Note the contrast within the same paragraph: the visual-verification instruction is genuinely non-inferable wiring and passes the core test cleanly — it is the model's only sanctioned channel into the verdict. Cut around it.
- Recommendation: Collapse the two narration spans to the invocation plus the pointer: "Jalankan `uv run scripts/ps-e2e-run.py <module-path> --versions <target> --browsers <browser>` dengan setelan flashlight sama seperti Lapis 2 (format spec & degrade: `--help`) — jangan nilai ulang outputnya." Keep the visual-verification block and the e2e-quickstart pointer verbatim. Saves ~80 tok.

#### leanness-4 — Headless section re-argues the `ready` design decision that --require's help text already makes verbatim

- Lens: leanness
- Location: `SKILL.md:45 (Mode headless, second paragraph — 129 tok of a 250 tok paragraph)`
- Evidence: The span from "Gerbang CI membaca ready" to "terekam di required_layers" is a near-verbatim duplicate of ps-aggregate.py's --require help string, which reads: "DEFAULT: KEEMPATNYA — siap-rilis berarti keempat lapis membuktikannya. Runner yang tak sanggup (tanpa Docker/browser) memang menghasilkan ready=false: itu jujur, bukan cacat. Sengaja menggating lebih sempit? Nyatakan di sini — pilihannya terekam di required_layers." SKILL.md:37 already directs the model to that --help. The content is also restated a third time at SKILL.md:39. Reproduced: aggregate emits ready, pass, required_layers, flashlight_conclusive, e2e_conclusive and layers_run in every run, so the reader holds the fields regardless. What remains is the author defending a design choice to a reader who cannot revisit it — canon's meta-explanation.
- Recommendation: Truncate to the operative fact and let --help carry the argument. Keep the surrounding return-contract sentences (the JSON shape and the blocked reasons) untouched — those are a real downstream contract with callers and pass the core test.
- Proposed smallest: Gerbang CI membaca **`ready`**: lolos + lapis yang di-`--require` tuntas (default keempatnya). Runner tanpa Docker/browser menghasilkan `ready: false` — jujur, bukan cacat module; `pass` tetap menampung temuan nyata. Menggating lebih sempit: nyatakan di `--require`.
- Predicted delta: Nothing. The rationale lives in --require's help at the point the flag is chosen, required_layers is echoed in the output as the record of the choice, and the pass-vs-ready distinction survives at SKILL.md:39. Saves ~80 tok. Route to variant eval to confirm — the one thing to watch is whether a headless caller still declines to invent a narrower --require, which the retained "default keempatnya" should hold.

#### enhancement-1 — Add: Plan-validate-execute is missing its load-bearing validation piece for authored E2E specs

- Lens: enhancement
- Location: `scripts/ps-e2e-run.py:246-274 (discover_scenarios), scripts/ps-plan-layers.py, SKILL.md:25`
- Evidence: Authored E2E specs (<module>/tests/e2e/*.json) are the only input a human hand-writes, and the only thing that lifts Layer 4 above e2e_smoke_only. discover_scenarios validates them at ps-e2e-run.py:852 — cheaply, and BEFORE the per-version Docker loop at :859 — but the result is only a note carried to the end of the run: the full flashlight boot x N versions x M browsers proceeds regardless, and the operator learns 'checkout.json: aksi tak dikenal (expect_visable) — dilewati' only in the final e2e_scenario_notes. The note names the offender but not the valid set, though SUPPORTED_ACTIONS (ps-e2e-run.py:84) is right there. Per the pattern, the validation script whose errors name the fix is the load-bearing piece, because it lets the author self-correct before the expensive step runs. Here the check exists but is neither a pre-flight nor self-correcting, so each typo costs a full multi-version Docker run to discover. ps-plan-layers.py — already the cheap deterministic pre-flight SKILL.md routes to at line 25 — stats mtimes and version coverage but never opens the spec files.
- Recommendation: Two script-side changes, zero SKILL.md tokens. (1) In discover_scenarios, append the valid vocabulary to the unknown-action note: f"... — sah: {', '.join(SUPPORTED_ACTIONS)}", and likewise name the expected shape on the 'tak ada steps list' branch. (2) Have ps-plan-layers.py load and validate <module>/tests/e2e/*.json (import the check from ps-e2e-run rather than duplicating the rule) and emit the notes in its plan output, so a malformed spec surfaces before any container boots. Do NOT make it a hard gate — the universal smoke still has value with a broken authored spec — so the run should proceed and stay honest via e2e_smoke_only. SKILL.md line 25 already tells the model to run ps-plan-layers first, so the routing cost is nil.

### Low (4)

#### architecture-3 — SKILL.md sits at 2987 of a 3000-token budget — in the warn tier with 13 tokens of headroom

- Lens: architecture
- Location: `SKILL.md (whole file); budget from bmad-workflow-builder/customize.toml:58-59`
- Evidence: 2987 tiktoken tokens against skill_md_token_desired 2000 / skill_md_token_budget 3000 — the principles' "between desired and budget" band, which warns and names the section most worth lifting. Any future clause pushes it over. The mass is Lapis 4 (SKILL.md:33), a single ~330-word paragraph carrying five distinct concerns (gate condition, prerequisite, invocation, visual verification, degrade), while references/e2e-quickstart.md is only 2714 tokens against a 9000 single-purpose budget. Note the fix in architecture-1 adds to :33, not subtracts. Separately, the prepass hardcodes [1500, 2500] (bmad-workflow-builder/scripts/prepass-prompt-metrics.py:205) instead of reading the declared scalars, so its own JSON reports this file as over budget — a builder-tooling mismatch, not a psm-validate defect.
- Recommendation: Lift the Lapis 4 visual-verification mechanics (--screenshot-dir invocation, review discipline, the write-back-to-adversarial channel) into a named block in references/e2e-quickstart.md, and leave the trigger plus the gotcha clauses from architecture-1 inline at SKILL.md:33 — the verdict-affecting rules stay where they fire, the mechanics get paid for only when the branch does. Fix the prepass to read {workflow.skill_md_token_desired}/{workflow.skill_md_token_budget} rather than its own constants.

#### architecture-4 — The inline `playwright install` prerequisite contradicts the reference's own version-match warning one parenthesis away

- Lens: architecture
- Location: `SKILL.md:33 vs references/e2e-quickstart.md:20-22, :30-32; scripts/ps-e2e-run.py:1-5 (PEP723 header)`
- Evidence: SKILL.md:33 states the prerequisite as bare `playwright install <engine dari psm_e2e_browsers>`. e2e-quickstart.md:30-32 warns that ps-e2e-run.py provisions playwright through its PEP723 header (dependencies = ["playwright"], unpinned -> latest) and that the install must run through the same playwright — `uv run --with playwright playwright install chromium firefox` (:20-22) — or the browser build will not match the one the script drives. Followed literally, the SKILL.md line invokes a system playwright that may be absent or a different version; the failure surfaces later as skipped_browser (a degrade that reads as "no browser on this machine") rather than as a version mismatch. The correct command is in the file routed on the same line, which softens this to a redundancy that happens to be wrong rather than a trap.
- Recommendation: Replace the inline command at SKILL.md:33 with the correct one (`uv run --with playwright playwright install <engine>`), or drop the command and keep only the routing — one source of truth either way. Pinning the PEP723 playwright dependency would remove the class of mismatch entirely.

#### architecture-5 — Build-time artifacts live inside the distributed skill directory, and the path scanner then scans its own output

- Lens: architecture
- Location: `skill root: .memlog.md (16957 tok), .analysis/ (13 dated rounds, git-tracked); references/e2e-quickstart.md:166-169`
- Evidence: The installed skill folder carries .memlog.md (55KB) and thirteen rounds of prior analysis reports, all tracked in git — git ls-files lists .analysis/** from 2026-06-25 onward. Neither is workflow content at root in the sense the principles forbid (SKILL.md, references/, scripts/, assets/ are clean and one level deep, and the live tree lints clean on paths), but e2e-quickstart.md:166-169 explicitly instructs copying this skill folder to other machines, and the copy is dominated by build artifacts. The same nesting is what makes scan-path-standards report 7248 phantom findings against its own prior reports — noise the analysis flow must be told to discount, as it was here. This is a bmad-workflow-builder convention, not a psm-validate authoring choice; raising it once as a tooling observation.
- Recommendation: Builder-side: write .analysis/ and .memlog.md to a location outside the skill package (e.g. _bmad-output/skill-analysis/<skill>/), or have the path scanner exclude dot-directories under the skill root. No change to psm-validate itself.

#### enhancement-2 — Add: the offer-the-fix for smoke-only E2E doesn't name where the fix is documented

- Lens: enhancement
- Location: `SKILL.md:39`
- Evidence: Same recurring shape as prior rounds, one layer up: references/e2e-quickstart.md already contains everything an operator needs to author a spec — a copyable scenario, the full action table, and the cross-version BO pattern without which configure scenarios fail on 1.7/8 — but the moment of need never opens that channel. Line 39 says to report e2e_scenario_notes and e2e_smoke_only and 'tawarkan perbaikannya: perbaiki/tulis skenario di <module>/tests/e2e/' without routing anywhere. The quickstart is routed only at line 33, and only for a different purpose (new-machine setup + the TDD loop). On a compacted turn the model taking up its own offer has the format nowhere in context and must rediscover it via --help.
- Recommendation: Append the pointer to the existing offer at line 39: '...perbaiki/tulis skenario di <module>/tests/e2e/ (format + pola BO: references/e2e-quickstart.md)'. Roughly 8 tokens, which the SKILL.md budget absorbs without a cut; if the budget is contested, pay for it out of the --extra-rules vs --rules ADD/REPLACE gloss at line 21, which is script-usage detail belonging in --help.
